"""Editorial specifications for the independent learning edition.

Every entry is intentionally topic-specific.  The build script turns these
specifications into Markdown, but the reasoning, example and boundary live
here so the generated lessons remain reviewable and reproducible.
"""


def S(id, order, chapter, title, question, bridge, mechanism, example, boundary,
      flow, arc, artifact, check):
    return {
        "id": id,
        "order": order,
        "chapter": chapter,
        "title": title,
        "question": question,
        "bridge": bridge,
        "mechanism": mechanism,
        "example": example,
        "boundary": boundary,
        "flow": flow,
        "arc": arc,
        "artifact": artifact,
        "check": check,
    }


SPECS = [
S(
    "00-introduction", 0, "导学", "导学｜把零散知识变成可解释、可验证的工程能力",
    "怎样学习一项后端技术，才能在真实故障和架构决策中用得出来？",
    "先区分“知道名词”和“拥有工程模型”。前者能复述定义，后者能说清输入、状态变化、失败路径、取舍和验证证据。后续每一课都按这五个对象来读。",
    "一项技术只有放进业务约束里才有意义。先问系统要保护什么指标，再画正常路径和失败路径；接着找出可观测信号、控制手段与副作用；最后用压测、演练或数据回放验证。这样得到的不是一句结论，而是一套能够迁移到新场景的推理方法。",
    "以服务注册为例：背下“客户端从注册中心获得节点”还不够。真正掌握意味着你能推演注册中心失联、服务端突然断电、客户端缓存过期三种状态，说明请求会落到哪里，并设计一次断网演练验证判断。",
    "这套学习法不承诺一次读完就会。工程能力来自反复预测、观察和修正；如果只收藏方案、从不写下假设和证据，知识仍然停留在记忆层。",
    ["业务目标", "正常/失败状态", "机制与取舍", "可观测证据", "演练与复盘"],
    ["先建立完整的课程地图，知道服务治理、数据库、消息系统、缓存和 NoSQL 分别解决什么问题。", "把原课中的技术问答改造成工程问题：什么条件下会坏，为什么坏，怎样证明修复有效。", "保留真实案例与警告，但不保留为表达效果服务的重复话术。", "每学完一个专题，用串联复盘把孤立知识点重新放回同一条请求链。"],
    "为每课建一张四列表：`假设 | 观测信号 | 控制动作 | 副作用`。如果某个方案只有动作、没有信号和副作用，就暂时不能进入生产设计。",
    ("为什么仅仅记住一个中间件的优点，仍不足以支持架构选择？", "因为架构选择依赖当前业务目标、故障模型和约束。优点只有和可验证的指标、失败代价及替代方案连接起来，才构成决策。"),
),
S(
    "01-service-discovery", 1, "服务治理", "服务发现｜在节点不断变化时找到真正可用的服务",
    "服务实例随时上线、下线或失联，客户端怎样持续拿到可用地址？",
    "把注册中心想成一份会变化的地址簿。服务实例是地址提供者，客户端是订阅者；心跳不是业务请求，而是用来推断节点状态的证据。",
    "完整机制包含注册、订阅、缓存、变更通知和客户端容错。心跳失败只能证明“此刻通信失败”，不能证明进程必然死亡，因此检测策略要在误杀与发现延迟之间取舍。客户端还必须保留本地节点表，并在一次调用失败后换节点；否则注册中心再快也填不上检测窗口。网络分区时再讨论一致视图和可用视图的选择，而不是平时泛泛地喊 AP 或 CP。",
    "订单服务有 A、B、C 三个实例，心跳间隔 5 秒。A 在第 1 秒断电，客户端最坏可能到第 6 秒才收到摘除通知。若第 3 秒请求仍选中 A，客户端应立刻把 A 放入本地隔离区并改投 B，而不是等待注册中心。这个 5 秒窗口就是必须由调用侧填补的空白。",
    "CAP 取舍只在发生分区、无法同时获得最新一致视图时成立。选择偏可用的注册中心也不等于节点列表永远正确；错误地址仍要靠超时、换节点和隔离处理。",
    ["实例注册并保持心跳", "注册中心维护节点状态", "客户端订阅并缓存", "调用失败时本地换节点", "恢复后重新纳入"],
    ["从上线与优雅下线建立服务发现的三角模型：实例、注册中心、客户端。", "用心跳分析突然宕机，明确误判概率和检测延迟无法同时归零。", "把检测延迟交给客户端容错，用换节点和短期隔离减少失败请求。", "最后讨论注册中心自身故障和分区取舍，小规模与大规模系统可以采用不同偏好。"],
    "演练表：拔掉 A 的网络，记录 `注册中心判定时间`、`客户端第一次失败时间`、`停止选择 A 的时间`、`恢复后重新接流量时间`。四个时间点比“支持高可用”更能说明系统行为。",
    ("心跳一次失败后为什么不能既保持使用该节点，又声称系统在做高可用？", "因为继续选中疑似节点会把检测窗口内的请求暴露给失败。更合理的是先在调用侧临时摘除，同时后台继续探测；恢复后再放回。"),
),
S(
    "02-load-balancing", 2, "服务治理", "负载均衡｜不是平均分流，而是持续做出更好的节点选择",
    "同一服务有多个节点时，怎样选择才能兼顾均衡、局部性和故障反馈？",
    "轮询、随机、哈希、最少连接只是“选择函数”。真正的负载均衡还要定义输入信号：静态权重、实时延迟、并发数、调用结果、缓存归属和节点健康状态。",
    "静态算法计算便宜但看不到节点瞬时状态；动态算法更贴近真实负载，却容易被噪声和慢反馈带偏。调用结果应更新节点的短期可用性，缓存亲和性则希望同一键尽量落到同一节点。一致性哈希通过环上后继节点减少扩缩容时的迁移，但虚拟节点数量和热点键仍会影响均匀度。选择算法必须和失败重试、缓存策略一起设计。",
    "节点 A/B/C 的稳定响应分别为 20/25/80ms。纯轮询会让三分之一请求长期变慢；最快响应算法若每次都选 A，又会把 A 压垮。可采用加权选择并用 EWMA 延迟做缓慢修正，例如权重 5:4:1；A 连续超时后临时降为 0，探测成功再逐步恢复。",
    "一致性哈希减少的是节点变动引发的键迁移，不保证请求量均匀，也不能代替健康检查。最少连接也不等于最少工作量：一个长查询和一个短查询都只算一个连接。",
    ["候选节点", "静态权重与实时信号", "选择函数", "调用结果反馈", "权重/可用性更新"],
    ["先比较轮询、随机、哈希、最少连接、最少活跃和最快响应各自观察到的信号。", "再把调用结果纳入选择：失败节点应短期退出，而不是机械地继续轮询。", "结合本地缓存讨论一致性哈希，理解扩缩容时为什么只迁移部分键。", "最后把算法放回业务：请求有无状态、节点是否异构、缓存是否昂贵，决定了选择标准。"],
    "记录每个节点的 `选择次数、成功率、P95、在途请求数、缓存命中率`。若总体延迟下降但单节点错误率升高，说明算法可能把压力集中到了“看起来最快”的节点。",
    ("为什么把所有请求都发给当前响应最快的节点，可能让总体响应越来越差？", "因为观测到的快可能只是它尚未承压。流量集中会增加排队并改变节点状态；选择策略需要反馈平滑、并发约束和探测流量。"),
),
S(
    "03-circuit-breaker", 3, "服务治理", "熔断器｜让失败依赖先休息，再用小流量确认恢复",
    "下游持续失败时，调用方怎样避免把线程、连接和重试继续耗在无效请求上？",
    "熔断器是带记忆的状态机，不是一次失败就永久禁用。关闭态正常放行；达到失败条件进入打开态并快速失败；冷却后进入半开态，只允许少量探测。",
    "判定条件可用连续失败数、滑动窗口失败率或慢调用比例。打开态保护的是调用方资源和已经过载的下游。半开态必须限制探测并定义恢复门槛，否则大量实例会同时恢复、再次压垮下游，形成“开—关抖动”。冷却时间、最小样本数和半开并发度要依据下游恢复速度调节。",
    "最近 100 次调用中 65 次失败，超过 50% 阈值后熔断 10 秒。第 10 秒只放 5 个探测请求：若 5 个都成功，不应瞬间恢复 100% 流量，可以按 5%→20%→50%→100% 逐级放量；任一级再次恶化就退回打开态。",
    "熔断不是降级本身。熔断决定“还要不要调用下游”，降级决定“不调用以后返回什么”。熔断也不能修复下游，它只改变流量和失败方式。",
    ["关闭态：正常放行", "窗口越过阈值", "打开态：快速失败", "半开态：有限探测", "渐进恢复或再次打开"],
    ["先用失败计数或失败率判断健康状态，说明最小样本数为何重要。", "再解释打开态如何阻止资源继续堆积，并为下游提供恢复空间。", "用半开态处理恢复，但限制探测并避免所有调用方同步放量。", "把抖动问题归因于阈值、冷却、探测规模和恢复斜率，而不是简单增加等待时间。"],
    "压测时画出四条时间序列：下游错误率、熔断状态、放行 QPS、调用方线程池占用。只有状态变化能对应资源释放，熔断才真正起效。",
    ("半开态为什么不能直接恢复全部流量？", "少量成功只能说明下游在低负载下可用，不能证明它能承受原流量。渐进放量用反馈验证容量，避免刚恢复就再次过载。"),
),
S(
    "04-degradation", 4, "服务治理", "服务降级｜资源不足时主动舍弃次要能力",
    "系统容量或依赖不足时，怎样让核心路径继续工作而不是一起崩溃？",
    "降级先要画业务优先级，而不是先选技术。核心路径、可延迟路径、可返回旧数据路径和可关闭路径必须由业务损失决定。",
    "降级可以发生在功能、数据新鲜度、计算精度或同步方式上。读写服务中可暂停非关键写入，快慢路径中可跳过慢速精确计算，外部依赖中可返回缓存或排队稍后处理。降级开关需要触发信号、审批边界、恢复条件和补偿流程；没有恢复与补偿的“临时关闭”会变成永久数据债务。",
    "大促时库存扣减、下单和支付是核心链路，历史报表、个性化推荐和退款进度刷新可延后。若数据库写入达到安全上限，推荐接口返回上一小时缓存；退款请求仍受理并写入队列，页面显示“已受理，稍后处理”，而不是直接丢弃。",
    "降级不等于返回成功。无法完成的写操作若伪装成成功，会破坏业务语义；应返回可解释状态并保证后续补偿。它也不同于限流：限流控制进入量，降级缩减单个请求消耗的能力。",
    ["识别核心业务", "检测容量/依赖风险", "切换到低成本路径", "记录待补偿工作", "条件恢复并回补"],
    ["先区分降级与熔断，明确一个处理替代结果、一个处理下游调用。", "从功能关闭、读写拆分、快慢路径和异步化四种形态理解降级。", "用退款等可延迟业务说明为什么停的是实时处理而不是业务承诺。", "最后补齐开关治理：谁能开启、何时恢复、遗漏数据怎样补偿。"],
    "为每个候选功能写降级卡：`业务损失、节省资源、返回语义、数据补偿、最大持续时间`。节省资源不明确或补偿不可执行的方案不能上线。",
    ("为什么把写请求统一返回“成功”不是一种安全降级？", "因为系统没有建立对应事实，调用方却会按成功继续推进，造成不可修复的不一致。安全降级必须保持语义诚实，并给出受理、排队或失败状态。"),
),
S(
    "05-rate-limiting", 5, "服务治理", "限流｜先算安全容量，再决定怎样拒绝流量",
    "限流阈值应该来自什么证据，而不是凭经验写一个整数？",
    "限流算法回答“流量怎样通过”，阈值回答“系统能安全处理多少”。容量要从瓶颈资源、目标延迟、并发模型、请求大小和安全余量推导。",
    "固定窗口简单但边界处可出现双倍突发；滑动窗口更平滑；漏桶稳定输出；令牌桶允许积累令牌以吸收短时突发。阈值需通过单机压测得到并在集群规模、故障冗余和流量分布下折算。限流后还要明确快速失败、排队、降级或重试提示，避免客户端无退避重试制造更大流量。",
    "某接口平均占用 CPU 20ms，4 核机器理论上限约为 `4×1000/20=200 QPS`。压测发现超过 160 QPS 后 P99 急升，因此以 140 QPS 作为单机安全值。10 台集群不能直接设 1400：若要容忍 2 台故障，应按 8 台计算约 1120，并继续为流量倾斜留余量。",
    "平均耗时推导的是粗略上限，不包含锁竞争、I/O 排队、GC 和长尾。令牌桶的突发能力也不是额外容量，只是把前一段空闲额度挪到当前使用。",
    ["压测定位瓶颈", "计算单机安全容量", "扣除故障与倾斜余量", "选择算法与突发额度", "拒绝/排队并反馈"],
    ["从固定窗口、滑动窗口、漏桶和令牌桶区分稳定速率与突发能力。", "明确限流对象可以是请求数、并发数、用户、租户或请求大小。", "用响应时间和核心数估算阈值，再用压测曲线修正理论值。", "最后设计拒绝后的客户端退避和降级，防止限流器外形成重试风暴。"],
    "容量报告至少保存 `QPS、并发、平均/P95/P99、CPU、连接池、错误率` 六组数据，并标出曲线开始非线性上升的位置。阈值来自拐点之前，而不是错误率达到 100% 的极限。",
    ("令牌桶每秒产生 100 个令牌并最多积累 20 个，为什么某一秒能通过 120 个请求却不能长期保持 120 QPS？", "前 20 个来自此前空闲时积累的额度。消耗完后长期补充速率仍是每秒 100 个，持续 120 QPS 会不断被拒绝。"),
),
S(
    "06-isolation", 6, "服务治理", "隔离｜先画清故障边界，再分配独立资源",
    "一个租户、任务或依赖变慢时，怎样避免耗尽全系统的公共资源？",
    "隔离不是简单多建几个线程池，而是让不同风险单元不能无上限地争抢同一资源。风险单元可按租户、业务、实例、机房、依赖或任务类型划分。",
    "隔离层级越靠近物理资源，故障边界越清晰但成本越高。机房和实例隔离适合强 SLA；线程池、连接池和信号量隔离成本低但仍共享 CPU、内存和网络。慢任务应独立队列与工作池，第三方依赖应拥有独立连接池和超时预算。隔离容量还要防止一边闲置、一边饥饿，可设置保底配额加可借用的弹性池。",
    "普通用户与 VIP 共用 100 个数据库连接。一次普通用户报表把连接占满，VIP 查询也会排队。改为 VIP 保底 20、普通 60、弹性 20：平时双方可借弹性连接；拥塞时 VIP 至少保有 20 个，普通请求在自己的边界内排队或降级。",
    "隔离不增加总容量，只控制容量被谁消耗。分出过多小池会降低利用率并增加配置负担；如果所有池最终仍争用同一个已饱和的数据库，隔离只能保护优先级，不能消除瓶颈。",
    ["识别风险单元", "选择隔离层级", "分配保底配额", "允许有限弹性借用", "单元内限流/降级"],
    ["从机房、实例、分组到线程池和连接池，比较隔离强度与成本。", "用 VIP 与普通流量解释为什么需要独立资源保底。", "把慢任务、制作库和线上库、第三方连接分别划入独立故障域。", "最后检查共享的底层瓶颈，避免只在上层分池却仍一起压垮数据库。"],
    "列出资源矩阵：行是业务单元，列是 CPU、线程、连接、队列、实例、存储。每个格子标明“独享、保底共享、完全共享”。矩阵能暴露表面隔离、底层仍共用的假边界。",
    ("给 VIP 单独线程池后，为什么仍可能被普通流量拖慢？", "如果两个线程池还共享已耗尽的数据库连接、CPU 或下游服务，线程隔离只隔开排队位置。需要沿调用链检查真正的瓶颈资源。"),
),
S(
    "07-timeout-control", 7, "服务治理", "超时预算｜把用户的一秒拆成整条调用链的可执行约束",
    "用户要求一秒内得到结果时，各个服务怎样共享而不是各自占满这一秒？",
    "超时是调用方停止等待的边界；它不保证被调用方工作已经停止。链路预算要包含网络、排队、重试和序列化，而不只是业务函数耗时。",
    "入口应携带截止时间，经过一跳就扣除已经消耗的时间，并为返回路径留余量。下游超时必须小于剩余预算，重试也只能使用剩余预算。业务代码需要在可中断点检查取消信号；数据库或第三方如果不支持取消，调用方释放等待后，下游仍可能继续消耗资源。监控应同时观察超时率、实际完成率和超时后残留工作。",
    "总预算 1000ms：网关与网络预留 100ms，服务 A 本地逻辑 80ms，三次数据库调用各 150ms，Redis 50ms，消息发送 80ms，返回预留 100ms，总计 860ms，还剩 140ms 抖动余量。如果把每次数据库超时都设为 500ms，第一跳就可能耗尽全链路。",
    "设置 1 秒客户端超时不等于用户一定 1 秒拿到成功结果，只能保证调用方最多等待到边界。超时也不自动回滚外部副作用，写操作需要幂等与状态查询。",
    ["入口生成截止时间", "逐跳扣除已耗时", "下游使用剩余预算", "可中断点响应取消", "记录超时后的残留工作"],
    ["先从单接口依赖耗时计算超时，而不是统一写死一个常量。", "再把预算扩展到调用链，要求每一跳传递截止时间。", "解释为什么取消信号无法自动终止任意业务代码，需要协作式中断。", "最后把重试放回预算：没有剩余时间就不应重试。"],
    "在追踪系统中给每个 span 记录 `deadline、remaining_budget、cancel_seen、actual_finish`。若调用方已超时但下游大量继续运行，说明系统只有等待超时，没有真正传播取消。",
    ("为什么 A 调 B、B 调 C 时，三者都配置 1 秒超时会破坏用户的一秒目标？", "每一跳都可能各等一秒，且忽略了前面已经消耗的时间。应传递同一个截止时间，由后续节点只使用剩余预算。"),
),
S(
    "08-third-party-calls", 8, "服务治理", "外部依赖｜把不可控接口包进可治理的边界",
    "第三方接口慢、错、变更或不可压测时，自己的核心流程怎样保持可控？",
    "第三方能力属于外部故障域。第一步是用适配器统一协议和错误语义，让业务代码不直接依赖供应商字段；第二步才是超时、重试、熔断和降级。",
    "客户端适配层应统一鉴权、序列化、错误码、幂等键和观测。只对明确可重试且幂等的失败做带抖动退避；对非关键结果可同步转异步，通过任务表或消息系统补偿。多供应商切换必须先解决语义差异、数据对账和切换条件。压测时用可配置桩模拟慢响应、错误比例和字段变更，不能直接冲击真实供应商。",
    "地址校验供应商 P99 为 800ms，而下单预算只有 500ms。可先使用本地规则完成基础校验并受理订单，把精确校验写入任务表异步执行；若发现异常再进入人工复核。这样不是假装第三方变快，而是重定义主流程对结果的依赖时机。",
    "重试不是默认安全动作。支付、发券等写接口如果没有幂等键，超时后重试可能重复执行。自动切换供应商也不保证结果等价，必须验证字段、精度和合规边界。",
    ["业务调用统一适配器", "规范错误与幂等语义", "超时/退避/熔断", "缓存或异步降级", "对账与人工补偿"],
    ["先建立一致性抽象，把不同供应商包装成稳定的内部契约。", "在客户端侧加入超时、限流、重试和熔断，但只对安全操作启用。", "补齐调用量、延迟、错误类型和业务结果的观测，不只看 HTTP 成功率。", "用同步转异步、多供应商和压测桩处理不可控依赖，同时保留对账与回补。"],
    "故障注入矩阵：延迟 2 秒、连续 500、响应成功但字段缺失、请求超时但实际成功、供应商配额耗尽。逐项写出主流程结果、重试行为、补偿入口和告警。",
    ("为什么第三方返回超时后立即重试，可能比直接失败更危险？", "超时意味着结果未知，不代表操作未执行。若接口有副作用且缺少幂等键，重试会产生重复支付、重复发券等问题。"),
),
S(
    "09-service-governance", 9, "服务治理", "服务治理全景｜从预防、止损到恢复构建闭环",
    "单个限流或熔断器都能工作，为什么系统仍可能在流量突增时整体失效？",
    "治理不是功能清单，而是闭环：先知道容量和依赖，再限制故障范围；故障发生时快速失败并保护核心链路；随后定位、恢复、补偿并把经验固化进变更流程。",
    "预防层包含容量评估、隔离、超时预算和变更门禁；止损层包含限流、熔断和降级；恢复层包含探测、渐进放量、重放与对账；证据层贯穿指标、日志、追踪和事件记录。每个手段都要对应明确风险，例如限流处理进入量过大，熔断处理下游持续失败，隔离处理资源争抢，不能互相替代。",
    "活动开始后请求从 2k 升到 10k QPS：网关先按已验证容量限流；推荐服务走缓存降级；订单服务与报表连接池隔离；支付依赖错误率超过阈值后熔断；告警定位到供应商后切换备用通道；恢复时按 5% 梯度放量并对账异步订单。",
    "“高可用”不是所有请求都成功，而是在故障和容量约束下保持约定的核心服务、明确失败语义并可恢复。自动化治理如果没有边界，错误规则也会自动扩大事故。",
    ["容量与变更预防", "隔离限制爆炸半径", "限流/熔断/降级止损", "观测定位根因", "渐进恢复与复盘"],
    ["先按容错、故障范围、发现与修复、变更规范四个维度整理治理目标。", "用一次真实问题按发现、计划、落地、效果和改进复盘，而不是罗列组件。", "把异步解耦和自动处理放进闭环，说明它们何时触发、失败后怎样回退。", "最终用业务 SLO 和故障演练验证组合方案，而不是以“接入中间件”作为完成标准。"],
    "建立治理控制表：每一行一个风险，每列填写 `检测信号、自动动作、人工确认、恢复条件、补偿任务、最大影响范围`。空白列就是事故发生时的未知行为。",
    ("为什么已经有熔断器，仍然需要限流和隔离？", "熔断处理的是依赖失败；流量可能在依赖健康时就超过容量，且不同业务会争抢公共资源。限流控制进入量，隔离限制争抢边界，三者处理不同风险。"),
),
S(
    "09a-mock-service-governance", 9.5, "服务治理", "服务治理专题复盘｜从一次故障串起九个控制手段",
    "面对一次“流量突增叠加下游抖动”的事故，怎样按因果顺序选择治理手段？",
    "复盘时不要按组件名字逐个背。先画请求从入口到依赖的路径，再标出流量、时间、资源和状态四种约束，治理动作自然会落在对应位置。",
    "入口过量由限流控制；不同租户争抢由隔离控制；每跳等待由超时预算控制；下游连续失败由熔断止损；核心路径资源不足由降级释放；节点变化由服务发现同步；节点选择由负载均衡反馈；外部供应商由适配层和补偿包围。所有动作最后汇入观测、恢复和变更治理。",
    "假设营销活动使流量涨 5 倍，第三方风控同时 P99 升到 3 秒。先按可承载 QPS 拒绝超额流量；VIP 与普通流量分池；风控调用 300ms 截止，超时订单进入人工复核；错误窗口触发熔断；恢复后半开探测；整个过程用订单受理率而不是 HTTP 200 率评估。",
    "专题串联不是“一次接入所有治理组件”。若没有明确风险，额外中间层会增加延迟和故障点。每个动作都要能指向一个已观察或已演练的失败模式。",
    ["画出请求链与核心目标", "标出容量/时间/依赖风险", "选择对应控制手段", "观察业务与资源信号", "恢复、补偿、复盘"],
    ["先回答系统的核心可用目标和允许牺牲的功能。", "沿请求链检查节点发现与选择，避免流量继续进入坏节点。", "再检查限流、隔离、超时、熔断、降级能否形成互补而非重复。", "最后把第三方、恢复放量、数据补偿和变更流程纳入同一份事故时间线。"],
    "复盘练习：画一张时间线，至少标出 `流量变化、第一条告警、第一次自动动作、核心指标最低点、恢复开始、补偿完成`。每个治理手段必须能落到一个时间点。",
    ("事故中数据库连接池已满，为什么先调大网关超时通常会让情况更差？", "更长等待会让更多请求和线程滞留，继续占用连接及内存。应先减少进入量、隔离或降级，再定位并扩展真实瓶颈。"),
),
S(
    "10-mysql-index", 10, "数据库", "InnoDB 索引｜从磁盘页、B+ 树到回表成本",
    "为什么一个合适的索引能把全表扫描变成少量页访问？",
    "数据库以页为 I/O 单位。B+ 树内部节点主要保存键和子页指针，叶子节点保存有序记录并相连，因此较高扇出降低树高，叶子链适合范围扫描。",
    "InnoDB 聚簇索引叶子保存整行，二级索引叶子保存索引列和主键。通过二级索引找非覆盖列需要拿主键回聚簇索引，这就是回表。联合索引按键顺序排序，只有能确定前缀范围的条件才能连续定位；覆盖索引则让所需列都在索引中。优化器是否使用索引还取决于选择性、估算成本和返回比例。",
    "表有一亿行，页大小假设 16KiB，内部节点平均可容纳约 500 个键。三级树理论上可覆盖约 `500³=1.25 亿` 个叶子位置，定位只需沿根—中间—叶子读取少量页。查询 `(tenant_id, created_at)` 时，先等值锁定租户再扫描时间范围，比只建 `created_at` 更能缩小区间。",
    "B+ 树不是所有数据库和所有场景的唯一选择；哈希、倒排、LSM 等结构有不同目标。“有索引”也不等于一定快：返回大比例数据、频繁回表或统计信息失真时，全表扫描可能更便宜。",
    ["查询条件", "定位联合索引前缀", "读取 B+ 树叶子范围", "覆盖返回或按主键回表", "合并并返回结果"],
    ["从页和 B+ 树结构解释树高、扇出与范围扫描，而不是只说“层数少”。", "区分聚簇与二级索引，理解二级索引为什么携带主键。", "用覆盖索引和最左前缀分析回表、排序和过滤成本。", "最后讨论写放大、空间和优化器选择，索引并非越多越好。"],
    "检查查询时记录 `possible_keys、key、key_len、rows、filtered、Extra`，再用真实执行统计验证估算。若扫描行数远大于返回行数，先检查索引前缀和过滤位置。",
    ("联合索引 `(tenant_id, created_at)` 为什么适合“某租户最近订单”，却未必适合只按 `created_at` 查所有租户？", "索引先按 tenant_id 排序。缺少前缀条件时，不同租户的时间段分散，无法直接形成一个连续的小范围。"),
),
S(
    "11-sql-optimization", 11, "数据库", "SQL 优化｜先定位多做了什么，再减少扫描、排序与回表",
    "一条 SQL 很慢时，怎样从证据而不是猜测开始优化？",
    "SQL 延迟由解析、等待、访问路径、行处理、排序/聚合、网络返回共同组成。执行计划是优化器的估算，慢日志和实际执行统计才是结果。",
    "先确认慢在数据库还是连接等待；再看扫描行数、返回行数、索引、临时表和排序；随后只改变一个变量，比较执行时间与资源。常见优化包括把过滤条件推到更早位置、建立覆盖索引、让排序顺序匹配索引、用游标替代深 offset、缩小返回列。COUNT、HAVING 和索引提示都应根据语义与数据分布验证，不能机械改写。",
    "订单表查询某商户最近 20 条已支付订单：`WHERE merchant_id=? AND status='paid' ORDER BY created_at DESC LIMIT 20`。索引只有 `merchant_id` 时要读取大量行再过滤排序；建立 `(merchant_id,status,created_at DESC)` 后可直接从小范围按序取 20 条。若还返回大字段，可能仍需回表。",
    "EXPLAIN 显示用了索引不代表性能已经好；扫描 500 万个索引项仍然很慢。强制索引可能短期修复统计误判，也可能在数据分布变化后变成新的问题。",
    ["慢日志定位语句", "实际计划识别额外工作", "形成单一优化假设", "改索引/SQL/数据模型", "对比延迟与资源"],
    ["先用慢日志和执行计划找证据，重点比较扫描与返回行数。", "按选择性、联合索引顺序和覆盖需求设计访问路径。", "分别处理 ORDER BY、COUNT、HAVING、深分页和大表变更，不把它们混成一个技巧。", "优化后在相同数据量与缓存状态下复测，并观察写入成本。"],
    "优化记录必须包含优化前后 SQL、数据规模、执行计划、P50/P95、扫描行数、返回行数和新增索引大小。只有延迟下降但写入变慢时，也要写出是否接受这个交换。",
    ("为什么 `type=range` 且 `key` 非空仍可能是一条坏计划？", "因为它可能扫描很大的索引范围、做大量回表或额外排序。访问类型和用了哪个索引只是线索，必须继续看实际扫描量与算子成本。"),
),
S(
    "12-database-locks", 12, "数据库", "InnoDB 锁｜锁住的是索引范围，不是抽象的一行数据",
    "同样一条更新语句，为什么有时只影响一条记录，有时却阻塞一大片范围？",
    "InnoDB 通过索引定位并加锁。记录锁针对索引记录，间隙锁针对记录之间的范围，临键锁组合二者；具体行为还受隔离级别、唯一索引和扫描范围影响。",
    "普通一致性读通常走 MVCC，锁定读和更新会申请锁。没有合适索引时，为找到目标需要扫描更多索引范围，也就可能持有更多锁。意向锁让表级操作快速知道行级锁状态。死锁来自事务以不同顺序等待资源，数据库会选择一个事务回滚；应用必须捕获并重试整个事务。乐观锁用版本条件检测冲突，适合冲突较少且可重试的场景。",
    "索引 `(tenant_id,score)` 上执行 `SELECT ... WHERE tenant_id=7 AND score BETWEEN 60 AND 80 FOR UPDATE`，锁定的不是“业务上 60 到 80 的几行”这么简单，而是实际扫描的索引记录及可能的间隙。另一个事务插入 score=70 可能等待；换成唯一等值查询时范围通常更窄。",
    "“行锁数据库”不代表任何 SQL 都只锁一行。锁范围取决于实际访问路径。乐观锁也不是不加锁，它把并发冲突检测推迟到写入条件，并可能需要重试。",
    ["SQL 选择访问索引", "扫描索引记录/范围", "申请记录或范围锁", "事务持有到提交/回滚", "冲突等待或死锁回滚"],
    ["先理解锁与索引的关系，再区分共享、排它、意向、记录、间隙和临键锁。", "说明锁通常在事务结束时释放，长事务会放大阻塞。", "用临键锁与不同加锁顺序推演死锁，并准备重试策略。", "最后比较悲观锁与版本号乐观锁，按冲突概率和副作用选择。"],
    "用两个会话手工复现：会话 A 开事务并执行范围锁定读；会话 B 分别尝试更新已有行、插入间隙和插入范围外记录。记录哪些阻塞，再用 `performance_schema.data_locks` 对照锁对象。",
    ("为什么给查询补上高选择性索引，可能同时改善性能和并发？", "它减少需要扫描的记录与范围，既少读页，也可能缩小加锁范围和持锁时间，从而降低等待。"),
),
S(
    "13-mvcc", 13, "数据库", "MVCC｜让读取选择可见版本，而不是和写入互相等待",
    "一行正在被修改时，普通查询为什么还能读到一个自洽的结果？",
    "MVCC 把“当前值”扩展成版本链。undo 信息能够重建旧版本；Read View 描述当前读取允许看见哪些事务版本。可见性判断选择版本，而不是把写锁去掉。",
    "事务更新记录时产生新版本并保留回溯信息。普通一致性读根据事务 ID、活跃事务集合和可见性边界沿版本链寻找可见版本。已提交读通常每条语句建立新的视图，因此同一事务后续语句可见新提交；可重复读通常在首次一致性读时建立视图并复用。锁定读读取较新的状态并参与锁竞争，和快照读不是一回事。",
    "余额最初 100。事务 T1 开始并读到 100；T2 把余额改成 80 后提交。若 T1 使用可重复读并复用旧视图，第二次普通读取仍可沿版本链看到 100；若使用已提交读，第二条语句的新视图可看到 80。T1 若执行 `FOR UPDATE`，则按锁定读语义处理。",
    "MVCC 不等于没有锁，也不保证所有查询都不阻塞。更新、删除、锁定读仍需要锁。长事务长期持有旧视图会阻碍旧版本清理并增加存储压力。",
    ["更新创建新版本", "undo 保留回溯信息", "读取建立 Read View", "沿版本链判断可见性", "返回自洽版本"],
    ["先从读写互斥的性能问题引出多版本。", "用版本链解释旧值从哪里来，而不是把 MVCC 当成内存快照。", "再比较已提交读与可重复读创建 Read View 的时机。", "最后区分快照读与锁定读，并讨论长事务对版本清理的影响。"],
    "用两个连接记录事务时间线：开始事务、第一次读、另一事务更新提交、第二次读、锁定读。分别在 READ COMMITTED 与 REPEATABLE READ 下执行，把结果写成可见性表。",
    ("为什么说 MVCC 解决的是“读哪个版本”，而不是“彻底取消并发控制”？", "写写冲突和锁定读仍需锁；MVCC 只让普通读取在版本链上选择一个可见版本，从而减少读写互阻。"),
),
S(
    "14-database-transactions", 14, "数据库", "事务持久性｜提交成功背后经过了哪些日志与刷盘边界",
    "数据库返回提交成功后，哪些故障可以恢复，哪些仍取决于配置和复制策略？",
    "事务的原子性、持久性和复制不是同一层承诺。undo 支持回滚与旧版本，redo 记录页修改用于崩溃恢复，binlog 服务于复制和时间点恢复；它们有不同职责。",
    "更新先修改缓冲页并生成日志。提交路径要协调 InnoDB redo 与 server 层 binlog，具体持久边界受刷盘参数和存储栈影响。WAL 的关键是先保证恢复所需日志稳定，再允许脏页稍后写回。主库本地提交也不等于副本已确认；如果业务要求跨节点耐久，需要明确同步确认或多数派语义及其延迟代价。",
    "订单状态从 pending 改为 paid。数据页尚未刷回表空间时机器断电，只要对应 redo 已按配置持久化，重启可重放恢复；若日志只停留在易失缓存中，突然掉电可能丢失。即使主库恢复无误，异步副本若尚未收到记录，主库永久损坏后的切换仍可能丢数据。",
    "“COMMIT 返回”不是跨所有硬件、所有副本和所有备份的绝对不丢。也不要为了加载数据在生产实例随意关闭 redo；那会改变崩溃恢复能力。",
    ["修改生成 undo/redo", "写入缓冲页", "协调 redo 与 binlog", "按配置刷盘并返回", "崩溃后分析/重做/回滚"],
    ["先区分 undo、redo、binlog 的目的和生命周期。", "按事务执行顺序理解 WAL 与脏页延迟写回。", "把提交、操作系统缓存、磁盘持久化和副本确认拆成不同边界。", "最后用故障注入验证配置：进程崩溃、操作系统重启、主机掉电和主库永久丢失不是同一种故障。"],
    "建立耐久矩阵：行是 `mysqld 崩溃、OS 重启、主机断电、磁盘损坏、机房丢失`，列是 `redo、binlog、副本、备份`。逐格写出是否能恢复和最大数据损失窗口。",
    ("为什么数据页尚未落盘，事务仍可能在重启后恢复？", "WAL 允许先持久化描述修改的 redo，脏页稍后写入。重启时根据日志把已提交修改重做到数据页。"),
),
S(
    "15-data-migration", 15, "数据库", "不停机迁移｜让全量复制、增量追平和切流组成状态机",
    "业务持续写入时，怎样把数据迁到新表或新库而不丢更新？",
    "迁移不是一次复制命令，而是新旧两个数据面之间的状态机。需要明确权威源、变更捕获、校验、切换门槛和回滚方向。",
    "先初始化目标数据，再通过 CDC 或双写追增量。全量期间发生的修改必须有可重放的变更序列。校验不能只比总行数，应按主键区间比较哈希、关键字段和缺失记录。切流前冻结状态转换：先让读流量灰度到目标，再改变写权威；双写顺序变化必须配合幂等和修复任务。迁移完成后仍要保留观察期和反向回滚通道。",
    "源表有 10 亿行，复制需要 6 小时。第 2 小时订单 42 从金额 100 改为 120。全量任务可能先复制旧值；CDC 记录随后把目标改为 120。切流前按主键分片比较版本号和字段哈希，发现差异就从权威源重放，而不是人工改一条算完成。",
    "双写本身不保证原子一致；一次写成功、另一次失败仍会分叉。校验也不是只在切换前做一次，切换过程和观察期都需要持续增量校验。",
    ["确定源端权威", "全量复制基线", "捕获并重放增量", "持续校验修复", "灰度切读写并保留回滚"],
    ["从全量初始化开始，明确复制期间的写入由增量通道承接。", "第一次校验修复建立可比较基线，再开启有顺序的双写或 CDC。", "切换读写权威时保持增量校验，避免一个开关同时改变所有流量。", "最后保留观察与回滚窗口，直到旧系统不再承担恢复职责。"],
    "迁移看板显示 `全量进度、CDC 延迟、差异条数、修复成功率、目标读流量、目标写流量、回滚开关状态`。任一差异无法解释时都不进入下一阶段。",
    ("为什么全量复制结束并且两边行数相同，仍不能立即切流？", "行数相同不代表字段值和版本相同，全量期间的更新也可能尚未追平。需要确认增量延迟归零并完成内容级校验。"),
),
S(
    "16-sharding-id", 16, "数据库", "全局 ID｜在唯一、趋势递增和路由信息之间做取舍",
    "分库分表后，怎样生成跨节点唯一且满足查询与写入需求的主键？",
    "ID 生成器要同时回答唯一性、可用性、排序性、吞吐、时钟依赖和信息暴露。没有一种格式在所有维度都最好。",
    "UUID 去中心化但随机写入会降低局部性且体积大；数据库号段可趋势递增，但依赖号段服务；雪花类方案把时间、节点和序列编码进整数，需要处理节点号冲突与时钟回拨。若高频查询按租户分片，可在 ID 中嵌入稳定的路由位，避免额外查映射表。批量预取号段提高吞吐，但进程崩溃会留下空洞；空洞通常不影响唯一性。",
    "设计 64 位 ID：41 位毫秒时间、10 位节点、12 位序列，理论上单节点每毫秒 4096 个。若需要按 32 个库路由，可从业务键哈希取 5 位嵌入约定位置。看到 ID 就能定位库，但也会暴露时间和规模，需要评估安全性。",
    "趋势递增不等于全局连续，唯一也不等于不可猜。依赖机器时钟的算法若回拨而没有防护，可能重复或暂停发号。不要把业务分片键直接裸露在外部标识中而忽略隐私。",
    ["明确 ID 约束", "选择时间/号段/随机来源", "分配节点与序列", "可选嵌入路由位", "监控回拨、冲突和耗尽"],
    ["比较 UUID、数据库自增、号段和雪花类算法的中心化与有序性。", "逐位解释时间、节点、序列分别解决什么，计算峰值容量。", "把分片路由信息纳入 ID，但说明安全和迁移代价。", "用批量取、提前取、singleflight 和局部分发降低号段服务压力。"],
    "验算表必须写出每个字段位数、最大值、单位和耗尽时间。例如 12 位序列是 0–4095；若一毫秒内超过 4096 个请求，生成器要等待下一毫秒或扩展位宽。",
    ("号段模式中出现 ID 空洞，为什么通常不是数据错误？", "生成器只承诺唯一和一定顺序，不承诺每个号码都被业务使用。进程拿到号段后崩溃会浪费号码，但不会产生重复记录。"),
),
S(
    "17-sharding-pagination", 17, "数据库", "跨分片分页｜为什么越往后翻，重复扫描和丢弃越多",
    "排序数据分散在多个分片时，怎样得到全局第 N 页而不让成本随页码爆炸？",
    "单库 offset 已经需要跳过前面的记录；跨分片还要从每个分片取候选、在中间层归并并再次丢弃。全局排序键必须稳定且能处理相同值。",
    "朴素方案让每个分片取 `offset+limit` 条，再归并前 N 条，深页会放大网络和内存。游标分页携带上一页的 `(sort_key,id)`，各分片只查询其后的数据，再做小规模归并；它不支持任意跳页但成本稳定。若产品必须跳页，可限制最大页、预计算中间结果、维护全局索引或交给更适合检索的系统。",
    "4 个分片查询第 1001 页，每页 20 条。朴素方式每片最多取 20020 条，共约 80080 条，只返回 20 条。若使用上一页最后的 `(created_at,id)` 作为游标，每片只取 20 个候选，最多归并 80 条。",
    "平均分页把 offset 平均分给各分片只在数据均匀且排序分布可预测时勉强成立，通常会漏数据或顺序错误。游标分页也要求排序键不可变或能接受变化带来的跳动。",
    ["请求全局排序页", "各分片按稳定键取候选", "中间层归并", "丢弃 offset 或使用游标", "返回结果与下一游标"],
    ["先推导全局查询为何需要每个分片提供候选。", "比较全量取 offset、平均分页和禁止深翻页的正确性与成本。", "用二次查询或中间表降低回表和归并数据量。", "最后根据产品需求选择游标、最大页限制或外部检索系统。"],
    "对每种方案记录复杂度：分片数 S、页偏移 O、页大小 L。朴素候选量上界约为 `S×(O+L)`；游标方案约为 `S×L`，这两个式子能直接解释扩分片和深分页的成本。",
    ("为什么给每个分片分配 `offset/S` 不能保证得到正确的全局第 N 页？", "各分片在目标排序区间内的数据量并不相等。平均跳过会在某些分片跳过过多、另一些过少，归并后位置就不再是全局第 N 页。"),
),
S(
    "18-distributed-transactions", 18, "数据库", "跨服务一致性｜在强协调、业务补偿和可用性之间选择",
    "一个业务动作跨越多个数据库或服务时，怎样处理部分成功？",
    "本地事务只能覆盖一个资源管理器的提交边界。跨资源后需要协调协议或把流程建模成可恢复状态机；选择取决于能否长时间锁资源、业务是否可补偿和一致性窗口。",
    "XA/两阶段提交用准备与提交协调多个资源，提供较强原子语义但协调器和锁持有影响可用性。TCC 把预留、确认、取消暴露给业务，侵入性高但控制清晰；Saga 把长事务拆成本地事务和补偿，接受中间状态；事务消息/Outbox 让本地数据与待发送事件同事务写入，再异步投递。所谓三阶段提交更多是理论扩展，不是常见业务默认方案。",
    "创建订单需要锁库存、扣余额、生成订单。TCC 可先 Try 预留库存与余额，再 Confirm；失败则 Cancel 释放。Saga 可先建待支付订单、预占库存、扣款，某一步失败按逆序补偿。补偿不是数据库回滚：退款可能产生新流水并需要人工处理。",
    "最终一致不等于随便异步。每一步仍需幂等、可重试、可观测和人工兜底。补偿也不一定能完全还原现实世界副作用，例如已经发送的短信无法“撤回”。",
    ["定义业务状态机", "执行本地事务并记事件", "协调确认或异步推进", "失败触发取消/补偿", "对账直到终态"],
    ["先用两阶段提交理解协调者、准备和阻塞成本。", "再比较 XA、TCC、Saga、AT/代理模式的侵入性和一致性。", "说明某些高性能场景可以禁止跨库事务，改用分片内聚合。", "最后用延迟执行或 Outbox 把短本地事务与长业务流程分开。"],
    "为业务状态画转移表：`当前状态、允许事件、幂等键、下一状态、补偿动作、超时动作、人工入口`。如果某个中间状态没有出路，就存在永久悬挂风险。",
    ("Saga 的补偿操作为什么不能简单理解成数据库 ROLLBACK？", "前面的本地事务已经提交，补偿是一个新的业务动作，可能失败、重试并留下审计记录，也可能无法完全撤销外部副作用。"),
),
S(
    "19-sharding-secondary-query", 19, "数据库", "非分片键查询｜把路由缺失的成本放到写入、存储还是查询",
    "数据按买家分片后，卖家、订单号和客服条件怎样定位记录？",
    "分片键决定数据放在哪里。查询不包含分片键时，路由层不知道目标分片，只能广播或依赖额外索引。解决方案是在不同成本之间移动工作，而不是凭空消除它。",
    "全局映射表用订单号定位分片，查询快但映射表成为关键依赖；按卖家冗余一份数据，读快但双写与一致性复杂；搜索系统支持多条件，但存在同步延迟；广播实现简单但成本随分片数增长。主键中嵌入路由信息适合按主键访问。所有派生副本都应携带版本或事件序号，以便重放与修复乱序更新。",
    "订单按 buyer_id 分 32 库。卖家后台按 seller_id 查询：写订单时同时写 `seller_order_index(seller_id,order_id,buyer_shard,version)`；查询先从索引表得到目标分片和主键，再批量回源。若索引消息延迟，页面要显示数据新鲜度并提供回查。",
    "广播不是一定错误，小规模分片、低频运维查询可能足够经济。冗余副本也不能依靠“最终会一致”而没有版本和校验；乱序事件会让旧值覆盖新值。",
    ["查询缺少分片键", "选择映射/冗余/搜索/广播", "定位候选分片", "回源并合并", "用版本校验派生数据"],
    ["从分片键选择和主查询模式开始，承认一个键无法优化所有访问。", "比较主键内嵌路由、中间映射表、二次分片和搜索系统。", "分析广播成本随分片数线性增长，确定低频场景是否可接受。", "最后处理双写、乱序、重试和校验，避免索引副本长期漂移。"],
    "决策表列出四种方案的 `读放大、写放大、存储、延迟、一致性、故障依赖`。例如映射表读两跳、写多一份；广播写零放大但读放大等于分片数。",
    ("为什么新增卖家维度的冗余表后，查询问题解决了，一致性问题却变难了？", "同一业务事实出现两个可独立写入的副本。部分失败、重试和事件乱序都可能让它们分叉，需要版本、幂等、对账和修复。"),
),
S(
    "20-sharding-capacity", 20, "数据库", "分片容量规划｜从增长、峰值与扩容成本反推库表数量",
    "应该分多少库、多少表，才能既承受未来规模又不过早付出复杂度？",
    "容量规划至少包含数据容量和请求容量两个维度，还要考虑热点、索引放大、副本、备份窗口与故障冗余。只按总行数除以单表上限会漏掉吞吐瓶颈。",
    "先预测目标周期内的记录数与字节数，再用压测确定单分片安全 QPS 和存储水位。选择 2 的幂常让哈希掩码和倍增扩容更方便，但不是业务定律。库解决连接、CPU 和实例吞吐，表解决单表数据与维护成本；二者不必同比增长。扩容应预先定义旧分片到新分片的映射和在线迁移步骤。",
    "当前每天新增 5000 万条，每条连索引约 600B，计划 3 年：原始增量约 32.9TB；考虑副本、空间水位和临时迁移空间后需求更高。若单表目标 5000 万行，至少约 1095 张表；再按单库安全写 QPS 与连接数决定每库放多少表，而不是直接做 1024 库。",
    "容量预测不是精确预言。过度分片会增加连接、元数据、跨片查询和运维成本；不足时扩容昂贵。目标是让下一次扩容有可执行路径，而不是一次性设计到永远。",
    ["预测目标期数据与流量", "压测单分片安全水位", "分别计算库数和表数", "加入热点与故障余量", "设计可重复的倍增迁移"],
    ["先明确为什么分片：容量、吞吐、维护窗口对应不同手段。", "区分分库和分表，避免把两个数字绑在一起。", "用增长率、记录大小、索引、副本和安全水位计算容量。", "最后推演扩容映射、数据迁移和回滚，2 的幂只是便利条件之一。"],
    "容量表按月份记录 `数据行数、字节、索引比、峰值 QPS、热点比例、备份时长、预计到达 70% 水位日期`。提前量应大于一次完整扩容演练和审批所需时间。",
    ("为什么“预计三年 32TB，所以平均分到 32 个库每库 1TB”仍不是完整规划？", "它没有验证每库读写 QPS、连接数、热点、索引与副本空间、备份和迁移水位；存储平均值也掩盖分布倾斜。"),
),
S(
    "21-database-architecture", 21, "数据库", "数据库架构全景｜把性能、可用性和一致性拆成可验证目标",
    "面对读写增长和故障要求，怎样组合参数、索引、复制、缓存与分片？",
    "架构设计先写 SLO 和工作负载：读写比例、事务边界、数据量、热点、允许延迟和可接受数据损失。没有这些条件，“读写分离还是分库分表”没有答案。",
    "第一层先修查询和数据模型，避免用更多机器掩盖低效 SQL；第二层调缓冲池、日志和连接等参数，并用指标证明；第三层用副本提供故障切换和可选读扩展，同时处理延迟；第四层才考虑缓存与分片。MySQL 8.0 已移除旧式 server query cache，因此“查询缓存”通常应指应用或代理层缓存，不应套用旧版本机制。",
    "业务峰值 20k QPS，读写 9:1。先发现 60% 读取集中在商品详情，可应用缓存削峰；剩余查询通过覆盖索引降低扫描；只读报表路由到可接受延迟的副本；核心写留在主库。若主库写 QPS 仍接近安全上限，再按租户分片，而不是一开始就拆库。",
    "副本读扩展不等于读到最新数据；分片不自动提高单事务性能；缓存不修复慢写。每层方案都会新增一致性和运维成本，必须用瓶颈证据决定是否进入下一层。",
    ["定义 SLO 与工作负载", "优化模型/SQL/索引", "参数与资源调优", "副本/缓存分担读取", "写入或容量瓶颈再分片"],
    ["先用工作负载说明整体方案，而不是从某个中间件开始。", "按证据调 buffer pool、日志和连接参数，区分容量与延迟目标。", "引入读写分离时明确复制延迟和读一致性。", "只有单机写入或容量无法继续扩展时再分片，并准备跨片查询与迁移。"],
    "架构评审必须回答：`主库永久丢失会丢多少数据（RPO）？多久恢复（RTO）？读副本多旧可接受？容量水位多少触发扩容？`。没有数字的“高可用、高性能”不能验收。",
    ("为什么读写分离可能降低主库读压力，却让业务正确性变复杂？", "副本异步复制会产生延迟。刚写完立即读副本可能看不到新值，因此需要主读、会话粘滞、版本等待或接受陈旧数据。"),
),
S(
    "21a-mock-database", 21.5, "数据库", "数据库专题复盘｜从一条慢查询走到容量与恢复设计",
    "如何把索引、锁、MVCC、日志、迁移和分片放进同一条数据生命周期？",
    "数据库知识不应按术语分割。一次请求从 SQL 进入优化器，经索引访问、并发控制、日志提交、复制，再走向备份与扩容；每一层都有性能和正确性边界。",
    "读慢先检查扫描、排序与回表；并发异常检查访问路径和锁范围；快照读结果由版本可见性决定；提交耐久由日志和刷盘边界决定；复制与读写分离引入新鲜度；迁移与分片改变路由并需要校验。复盘时沿一条订单数据的写入、查询、修改、复制、迁移和归档逐层追踪。",
    "订单表从 1 亿增长到 20 亿：先用联合覆盖索引修复查询；热点更新发生死锁时统一加锁顺序；报表走延迟可接受的副本；主库写入接近上限后按租户分片；迁移使用全量+CDC+校验；全程定义提交、复制和备份各自能承受的丢失窗口。",
    "专题复盘不是把所有技术一起上。每引入一层都要指出它解决的瓶颈、引入的状态和回退方式；否则复杂度会超过收益。",
    ["SQL 与索引访问", "锁/MVCC 并发", "日志提交与复制", "迁移校验", "分片容量与路由"],
    ["用执行计划确认访问路径，再解释它如何影响锁范围。", "用版本链区分快照读和锁定读，连接到事务隔离。", "把 redo、binlog、复制和备份拆成不同耐久层。", "最后以容量证据触发迁移与分片，并设计非分片键查询和扩容。"],
    "画一条订单生命周期：创建时写了哪些日志；查询用了哪个索引；并发修改拿什么锁；副本何时可见；迁移事件怎样重放；分片后如何用订单号路由。任一问答不出就回到对应课程。",
    ("一条 SQL 已经通过索引变快，为什么仍要检查它在事务中的加锁范围？", "性能与并发正确性相关但不等价。访问路径决定扫描及可能加锁的索引范围，范围查询仍可能阻塞插入或与其他事务形成死锁。"),
),
S(
    "22-message-queue", 22, "消息队列", "消息队列入门｜把生产速度与处理速度解耦",
    "什么时候应该让请求直接调用下游，什么时候应该先写入消息队列？",
    "同步调用让上游同时等待下游的容量、延迟和可用性。消息队列在两者之间保存事实，使生产者先完成自己的提交，消费者按自身速度处理；代价是系统多了消息状态和最终一致窗口。",
    "队列主要提供三种能力：异步缩短前台等待、削峰把瞬时流量摊平、解耦让多个消费者独立演进。生产者应发送已经发生的业务事实或明确命令，并拿到可解释的确认；消费者处理后再提交消费位置。失败会重试，因此业务不能假定一条消息只出现一次。",
    "秒杀入口 10 秒收到 20 万个请求，订单库稳定能力为每秒 8000 次写入。入口先做资格与库存令牌校验，再把 12 万个有效请求写入队列；消费者以每秒 7000 个的安全速率创建订单，约 18 秒处理完。用户看到的是“排队中”状态，而不是让 20 万个连接一起压垮数据库。",
    "队列不能增加下游的真实处理能力，只能暂存差速。若平均生产速度长期高于消费速度，积压仍会无限增长。异步也不适合必须立即得到强一致结果的步骤，例如只有扣款成功后才能当场交付的高风险资产。",
    ["请求完成本地校验", "可靠写入业务事实", "Broker 持久化并确认", "消费者按能力处理", "业务提交后确认消息"],
    ["从同步链路的等待和级联故障开始，说明解耦的对象其实是时间。", "再用秒杀流量解释异步、削峰和解耦分别解决什么。", "沿发送确认、Broker 保存、消费确认追踪一条消息的状态。", "最后补上重复、乱序、积压和一致性窗口，避免把队列理解成无成本加速器。"],
    "为一条订单消息记录五个时间：业务提交、发送开始、Broker 确认、消费开始、消费完成。用 `消费完成-业务提交` 衡量端到端延迟，用生产与消费速率差预测积压，而不是只看请求接口变快。",
    ("接口返回很快，为什么仍不能说明引入消息队列后系统吞吐已经提高？", "接口只是把工作转移到了队列。若消费者与下游的稳定吞吐没有提高，工作仍在积压；必须同时观察生产速率、消费速率、积压量和最老消息年龄。"),
),
S(
    "23-delayed-messages", 23, "消息队列", "延迟任务｜让消息在正确的时间变得可消费",
    "订单 30 分钟未支付自动关闭，怎样做到可恢复、可取消且不会被重复执行？",
    "普通队列表达的是先后顺序，不一定支持任意消息的独立到期时间。延迟任务需要同时保存业务键、目标时间、版本和当前状态；调度器只负责在到期时触发，最终动作仍要回到业务状态机校验。",
    "可用方案包括：支持延迟能力的专用队列；按延迟档位建立多个主题；数据库时间索引加扫描器；时间轮或有序集合加迁移队列。Kafka 的核心是分区追加日志，不应假定它原生提供任意精度的单消息延迟；常见做法是延迟主题分桶或独立调度服务。无论哪种方案，取消通常不是删除消息，而是到期时检查订单版本和状态后忽略失效任务。",
    "订单 O42 在 10:00 创建，目标执行时间 10:30，版本为 3。调度表保存 `(O42, 10:30, 3, WAITING)`；扫描器在 10:30 抢占任务并发出关闭命令。若订单 10:12 已支付并变为版本 4，消费者发现版本不匹配，记录 `SKIPPED` 而不关单。扫描器崩溃后租约到期，另一实例可以重试。",
    "到期时间精确到秒不等于一定在该秒完成；积压和下游故障都会带来执行延迟。只把定时任务放进进程内存会在重启时丢失；只依赖删除消息实现取消，也很难覆盖消息已经被投递或复制的情况。",
    ["写入目标时间与版本", "持久化等待任务", "到期扫描或时间轮触发", "消费者重读业务状态", "执行或跳过并记终态"],
    ["先区分投递延迟与业务执行时刻，不把定时器等同于最终动作。", "比较延迟主题、数据库扫描、时间轮和专用队列的精度与恢复能力。", "用订单版本解释支付后怎样让旧关闭任务自然失效。", "最后设计抢占租约、重复触发、积压告警和人工补偿。"],
    "建立延迟误差直方图：`实际开始时间-目标时间`。同时统计 WAITING、RUNNING、DONE、SKIPPED、RETRY 各状态数量，以及最老到期未执行任务，才能区分正常抖动和调度停摆。",
    ("为什么支付成功后不一定要从队列中物理删除那条关单消息？", "删除可能竞争失败，也可能消息已在途。让任务携带订单版本，到期后重读业务状态并幂等地跳过，通常比依赖精确删除更可靠。"),
),
S(
    "24-message-ordering", 24, "消息队列", "消息顺序｜先定义谁和谁必须保持先后",
    "怎样保证同一订单的创建、支付和退款按因果顺序处理，而不同订单仍可并行？",
    "全局顺序会把所有工作压到一条串行通道，代价很高。大多数业务真正需要的是同一实体内有序：同一个订单键进入同一分区，由同一消费序列处理；不同订单分布到不同分区并行。",
    "生产者用稳定业务键选择分区，Broker 只承诺分区内日志位置递增。消费者组中一个分区在同一时刻交给一个成员，但消费者内部若把消息交给无序线程池，完成顺序仍可能颠倒。扩分区会改变某些键的映射，切换期需要版本校验、固定分区或双阶段迁移。跨分区事件只能靠业务序号、状态机或协调层恢复因果。",
    "订单 A 的事件序号 7、8、9 和订单 B 的 3、4 分别按 order_id 哈希。A 都进入分区 2，B 进入分区 5；两个分区并行消费。A 的退款事件 9 若因重试先到，消费者看到当前版本仍为 7，就暂缓或拒绝，等版本 8 的支付事件完成后再执行。",
    "同一生产者按同一键发送不代表端到端一定有序：重试配置、多个生产者、分区扩容、异步执行和失败重放都可能改变观察顺序。业务状态机必须拒绝不合法跳转，不能把正确性全部托付给队列。",
    ["识别有序实体与业务键", "相同键路由同一分区", "分区日志按位置读取", "实体内串行提交状态", "版本校验乱序与重放"],
    ["先缩小顺序范围：全局、分区还是业务实体。", "再说明稳定键如何换取实体内顺序和实体间并行。", "沿生产重试、分区扩容和消费者线程模型找出二次乱序。", "最后用事件序号与状态机验证顺序，而不是盲信到达时间。"],
    "测试时为每个实体生成连续序号并随机注入重试、消费者重启和分区再均衡。最终断言不是“日志看起来有序”，而是每个实体的已提交版本连续，非法状态转移计数为零。",
    ("同一 order_id 已固定写入一个分区，为什么消费者仍可能把后到的事件先落库？", "消费者可能把两条消息交给并行线程，前一条执行更慢；也可能先失败后重试。需要实体内串行、分区内有序执行器或版本条件更新。"),
),
S(
    "25-message-backlog", 25, "消息队列", "消息积压｜先计算差速，再决定扩容还是降载",
    "积压出现时，怎样判断是短时波峰、消费者变慢，还是下游已经超过容量？",
    "队列像水库，积压变化率近似等于生产速率减消费速率。消息条数只说明存量，最老消息年龄才直接反映用户等待；平均速度会掩盖分区热点和单条慢任务。",
    "先看各分区生产速率、消费速率、Lag 与最老年龄，再分解消费者耗时到反序列化、业务计算和下游 I/O。若只是短峰且可在 SLO 内消化，可以观察；若消费者有空闲分区，增加实例无效；若分区足够且下游有余量，增加消费者或批处理；若下游已饱和，则先限流、降级或暂停非核心生产，再修下游。临时扩容后还要安全缩回，避免重平衡风暴。",
    "生产速度 20,000 条/秒，消费速度 12,000 条/秒，差速 8,000 条/秒。持续 15 分钟会积压 720 万条。若扩容后消费达到 30,000 条/秒，净消化速度 10,000 条/秒，还需约 12 分钟清空；用户最长等待不会在扩容瞬间归零。",
    "消费者数量超过可分配分区数不会继续增加并行度。只提取消息后快速提交 offset、再异步处理，可能让 Lag 变小却把风险转移到进程内存；进程崩溃时工作丢失。",
    ["发现最老消息年龄上升", "计算生产与消费差速", "定位分区/代码/下游瓶颈", "扩消费或限制生产", "追平后验证并安全缩容"],
    ["先用差速公式估算积压增长和清空时间。", "再按分区观察热点，确认扩实例是否有可用并行度。", "分解单条耗时，辨别消费者代码和下游容量。", "最后制定限流、降级、扩容、重放和缩容顺序。"],
    "告警至少联合三个量：Lag 条数、最老消息年龄、消费错误率。演练中记录扩容前后消费速率，代入 `清空时间=积压量/(消费速率-生产速率)`，预测值应与实测同一数量级。",
    ("Lag 条数突然下降，为什么仍不能立即宣布故障恢复？", "消费者可能提前提交了位置，或只是把消息搬到内部队列。必须确认业务落库成功率、最老业务任务年龄和下游状态，而不只看 Broker offset。"),
),
S(
    "26-message-durability", 26, "消息队列", "消息可靠性｜逐段证明一条消息没有静默消失",
    "从业务提交到消费落库，一条消息在哪些边界可能丢失，怎样逐段封住？",
    "“发送成功”可能只是写进客户端缓冲；“Broker 收到”也不一定已落在足够副本；“消费到了”更不等于业务副作用已经提交。可靠性要沿每个确认边界逐段定义。",
    "生产端应在本地事实与待发送事件之间建立原子关系，常见做法是 Outbox；客户端等待符合目标的 Broker 确认并允许幂等重试。Broker 通过复制、选主和持久化降低节点故障损失。消费端先完成业务事务，再提交消费位置；若两者无法原子化，就接受重复并让业务幂等。Kafka 的事务可以协调其体系内的读写与 offset，但外部数据库、短信等副作用仍需要幂等或事务消息桥接。",
    "订单事务同时写 `orders(O7)` 与 `outbox(E91, NEW)`。投递器把 E91 发往 Broker，收到确认后标记 SENT；标记前崩溃会再次发送。消费者用 E91 作为唯一键写入发货任务，事务成功后提交 offset。无论生产或消费在哪个空隙崩溃，最坏结果是重复而不是静默丢失。",
    "更强确认通常牺牲延迟和可用性；复制也不是备份，错误删除可能被同步到所有副本。把“至少一次”宣传成“恰好一次业务效果”会掩盖外部副作用和人工操作。",
    ["业务事务写事实与 Outbox", "投递并等待 Broker 确认", "Leader 与副本保存日志", "消费者事务落业务结果", "提交位置并对账缺口"],
    ["先列出客户端缓冲、网络、Broker、复制、消费确认五段风险。", "用 Outbox 解决业务提交与发送之间的空隙。", "用复制和确认策略说明 Broker 能承受何种故障。", "最后把消费提交放到业务成功之后，以幂等承接不可避免的重复。"],
    "构造故障矩阵：在 Outbox 提交前后、Broker 确认前后、业务事务前后、offset 提交前后分别杀进程。每次恢复后都要满足“业务效果一次、消息可追踪、无永久 NEW 状态”。",
    ("为什么消费者先提交 offset、再写业务数据库是一条危险路径？", "提交后 Broker 会认为消息已完成。如果随后进程崩溃，消息不会再投递，而业务结果尚未写入，形成静默丢失。"),
),
S(
    "27-duplicate-consumption", 27, "消息队列", "重复消费｜把至少一次投递变成一次业务效果",
    "消息可能重复到达时，怎样保证余额、库存和通知不会重复产生副作用？",
    "重试是可靠投递的正常组成部分，不是异常角落。幂等的含义不是代码执行一次，而是同一个业务请求重复执行后，可观察结果与执行一次相同。",
    "优先选择天然幂等的状态覆盖或条件更新；非幂等动作使用业务幂等键和数据库唯一约束。去重记录与业务结果应在同一本地事务内提交，否则“记录已处理但业务失败”或相反仍有空隙。Redis 和布隆过滤器可在前面挡掉大量明显重复，但缓存丢失、过期或误判不能作为资金类最终凭据。通知等外部副作用要向对方传幂等键，或采用待发送表与状态机。",
    "扣款消息 E77 到达两次。事务先插入 `processed_event(E77)`，唯一索引只允许一次，再执行 `UPDATE account SET balance=balance-100 WHERE ...`。第一次共同提交；第二次插入冲突后读取已有结果并返回成功。若扣款与去重分开提交，任一中间崩溃都可能多扣或漏扣。",
    "用消息的随机投递 ID 去重可能挡不住业务重发，因为同一业务动作可能生成新消息 ID。去重记录也不能无限保留而不评估业务重试窗口；过早过期会让迟到副本再次生效。",
    ["收到消息与业务幂等键", "检查或插入去重凭据", "同事务执行条件更新", "提交唯一业务结果", "重复请求读取既有结果"],
    ["从至少一次投递推导重复为何不可避免。", "区分消息 ID、请求 ID 和业务唯一键，选择真正代表一次动作的键。", "把去重与业务副作用放在同一事务中，封闭崩溃空隙。", "最后为缓存加速、外部副作用和去重保留期补上边界。"],
    "并发压测同一个幂等键 100 次，断言业务表只发生一次有效状态转移，所有调用返回相同业务结果，并且唯一冲突可观测。再把消费者杀在事务前后，验证恢复结果不变。",
    ("为什么只用 Redis `SETNX` 记录消息已处理，仍不足以保护余额扣减？", "Redis 标记与数据库扣款不是同一事务。标记成功后数据库失败会漏扣，数据库成功而标记丢失会重扣；缓存只能加速，最终约束应靠业务事务和唯一键。"),
),
S(
    "28-design-a-message-queue", 28, "消息队列", "从零设计消息队列｜先定义日志，再补齐确认、消费与复制",
    "如果自己实现一个最小可用消息队列，哪些状态不可缺少？",
    "队列的核心不是一个内存列表，而是可追加、可恢复的持久日志，以及生产位置、复制进度和消费位置。先把单分区正确性做清楚，再扩展分区、消费组与高可用。",
    "Broker 接收记录后追加到分段日志，稀疏索引帮助按 offset 定位；生产请求带序列号以识别重试。主题拆成分区提供并行度，每个分区选 Leader 并复制到副本。消费者组通过租约或协调器分配分区，消费位置独立保存，因此同一日志可以被不同组重复读取。保留策略按时间或大小清理日志，与“某个消费者读完即删除”不同。元数据、选主和复制截断都需要明确一致性协议。",
    "一个主题有 4 个分区，每个分区 3 副本。生产者把键 K 写到分区 2 的 offset 108，Leader 追加并等两个副本达到确认条件后回复。消费组 G 的成员 C1 从 108 处理，业务成功后提交 109，表示下一条读取位置。另一消费组 H 仍可从自己的位置读取同一条记录。",
    "最小实现可以暂不支持跨分区事务、动态扩容和精确一次，但不能省略崩溃恢复与校验。只依赖文件系统缓存却宣称每条已持久化，或只复制数据却没有一致的高水位，都会在选主时暴露数据分叉。",
    ["追加分段日志并分配 offset", "复制到满足确认的副本", "按主题分区暴露读取", "协调消费组与保存位置", "按保留策略清理并恢复"],
    ["从单机追加日志、校验和与崩溃恢复开始。", "加入 offset 和稀疏索引，解释读取与保留不互相绑定。", "再用分区扩吞吐、消费组分工，用副本承受节点故障。", "最后列出确认、高水位、选主、再均衡和幂等生产的状态机。"],
    "最小验收包括：随机断电后日志只能丢到承诺边界；损坏尾段能检测并截断；同一生产序列重试不重复追加；消费组重平衡后一个分区不能同时由两个活跃租约提交。",
    ("为什么消费组提交的 offset 通常表示“下一条要读的位置”，而不是把消息从日志中删除？", "日志保留与某个消费组进度解耦，多个消费组可独立重放。同一条记录能被不同用途读取，offset 只记录组自己的游标。"),
),
S(
    "29-kafka-performance", 29, "消息队列", "Kafka 吞吐来源｜批量、追加日志与并行分区怎样叠加",
    "Kafka 为什么能在可靠保存消息的同时维持高吞吐？",
    "高吞吐不是“顺序写”一个原因，而是端到端减少每条消息的固定成本：生产端批量、网络与压缩摊销、Broker 追加日志、操作系统页缓存、读取传输路径和分区并行共同作用。",
    "生产者把多条记录组成 batch，一次系统调用和网络往返承载更多数据，压缩也能跨记录获得更高比例。Broker 以追加方式写分区日志，避免随机更新；页缓存让读写复用操作系统成熟的缓存与回写。合适平台可利用零拷贝类路径减少用户态复制，但具体机制受操作系统、加密和版本影响，不能作为永恒口号。分区让不同磁盘和节点并行，消费者也能按分区扩展。",
    "若单条 1KB 消息每次独立请求，10 万条会产生 10 万次协议与系统调用开销。改为每批 500 条，只需约 200 个批次；即使等待批次增加 5ms 延迟，吞吐和压缩率可能大幅改善。把 24 个分区均匀放到 6 个 Broker，则写入与消费可并行，但热点键仍可能只压在一个分区。",
    "更大的 batch 和等待时间会增加尾延迟与内存；更多分区带来文件、复制、选主和再均衡成本。吞吐优化不能以降低确认级别、容忍数据丢失为默认代价，可靠性目标必须先固定。",
    ["生产端聚合批次", "网络传输与压缩摊销", "Broker 追加分区日志", "页缓存与读取路径复用", "多分区并行消费"],
    ["先把单条消息的固定成本拆成网络、系统调用、复制和存储。", "说明批量与压缩如何摊薄这些成本。", "再解释追加日志、页缓存与传输路径为什么避免额外搬运。", "最后用分区扩并行，同时计算热点和元数据代价。"],
    "基准测试同时报告消息条数与字节吞吐、P50/P99 延迟、CPU、磁盘吞吐、网络、压缩比和副本延迟。只给“每秒多少条”而不说明消息大小、确认配置和硬件，结果无法复现。",
    ("把批次从 100 条扩大到 1000 条，为什么吞吐可能提高而 P99 延迟变差？", "消息需要等待批次凑齐或达到超时，单批处理时间和重试代价也更大。批量摊销固定成本，但会引入排队等待和更大的故障单位。"),
),
S(
    "30-kafka-practice", 30, "消息队列", "Kafka 调优｜沿生产、Broker、消费三段寻找真正瓶颈",
    "吞吐不够或延迟升高时，怎样调优而不是盲目改参数？",
    "调优先固定可靠性和延迟目标，再用指标判断瓶颈在生产者、Broker 资源、复制还是消费者。参数是控制杆，不是答案；一次只改变少量变量并保留回退基线。",
    "生产端关注批次大小、等待时间、压缩、缓冲与在途请求；Broker 关注分区分布、磁盘与网络、页缓存、请求队列、复制落后；消费端关注每批处理、拉取大小、并发模型、提交语义和下游耗时。若分区热点，增加消费者无效；若磁盘空闲而 CPU 被压缩打满，应调整算法或把成本移到合适一侧；若系统开始交换内存到磁盘，尾延迟通常会恶化，应控制堆与进程内存并保留页缓存。",
    "基线为 80MB/s、P99 180ms，Broker 磁盘仅 35% 但生产者 CPU 95%，平均批次只有 8KB。先把批次目标增至 64KB、等待从 0 调到 5ms并评估压缩；吞吐升到 140MB/s，P99 210ms。若业务预算是 250ms，这个交换可接受；若预算 100ms，就需要降低等待或扩生产端。",
    "不要同时修改十几个参数后用一次峰值证明成功。增加分区可能短期提升并行度，却增加复制、文件句柄和恢复时间；关闭可靠确认换来的吞吐必须明确记为可靠性降级。",
    ["固定 SLO 与可靠性基线", "采集生产/Broker/消费指标", "定位最饱和资源", "单变量调整并压测", "对比尾延迟、吞吐与故障恢复"],
    ["从端到端延迟分解开始，避免只盯 Broker。", "按生产者、Broker、消费者三段列指标和容量。", "根据 CPU、磁盘、网络、分区倾斜选择控制杆。", "最后用故障与恢复测试验证调优没有破坏确认和重放语义。"],
    "维护实验日志：工作负载、消息大小、分区与副本、确认方式、单次改动、吞吐、P99、资源利用率、积压恢复时间。任何优化都要能从前后两行数据中解释。",
    ("Broker 磁盘利用率不高，增加磁盘数量却没有提高吞吐，下一步应检查什么？", "检查生产者批次与 CPU、网络上限、分区热点、复制等待、消费者或下游。磁盘空闲说明它未必是当前约束，扩非瓶颈资源不会提高端到端吞吐。"),
),
S(
    "30a-mock-message-queue", 30.5, "消息队列", "消息队列专题复盘｜沿一条事件证明可靠、可序与可恢复",
    "怎样把延迟、顺序、积压、可靠性和幂等串成一条完整的事件生命周期？",
    "专题复盘不背产品参数，而是跟踪一条业务事实：它为何产生、何时可见、存在哪里、由谁处理、失败如何恢复、重复怎样收敛，以及积压时怎样保护下游。",
    "生产者先在本地事务中保存事实与 Outbox，再按业务键发送到分区；Broker 满足确认条件后回复。消费者按分区读取，业务事务用幂等键落库，再提交位置。延迟动作携带目标时间与版本；积压用差速和最老年龄判断。任何阶段故障都应能回答消息处于哪个可观察状态，恢复后是重试、跳过还是人工处理。",
    "支付事件 P88 与订单 O12 同事务写入，投递到 O12 所在分区。库存消费者成功处理但提交 offset 前崩溃，消息重放；唯一键 P88 让第二次读取已有结果。通知消费者下游超时，进入有界重试和死信观察。生产突然高出消费 5000 条/秒，10 分钟积压 300 万条，系统限制非核心通知并优先保证库存链路。",
    "一条链路“最终成功”不代表设计合格。若无法定位消息、没有重试上限、死信无人处理、顺序依赖隐藏在并发线程中，下一次故障仍会变成数据悬案。",
    ["本地事实与 Outbox", "分区路由和 Broker 确认", "复制与可读位置", "幂等业务事务和 offset", "积压/死信/对账收敛"],
    ["先画出业务事务与消息发送的原子边界。", "再标记分区键、复制确认和消费组进度。", "注入重复、乱序、消费者崩溃与下游超时，说明每种恢复路径。", "最后用积压公式和死信处理人把运行闭环补齐。"],
    "任选一个真实事件，填写 `event_id、business_key、partition、producer_attempt、broker_offset、consumer_attempt、business_result、next_retry_at、final_state`。若日志无法关联这些字段，先补可观测性再谈可靠消息。",
    ("为什么消息系统的最终验收要看业务对账，而不只是 Broker 的成功率？", "Broker 只能证明消息在其边界内被保存和读取，不能证明外部数据库或接口产生了正确且唯一的业务效果。对账才能发现跨边界的漏做、多做和永久中间态。"),
),
S(
    "31-redis-expiration", 31, "缓存", "Redis 过期机制｜键到期后为什么不一定立刻消失",
    "给键设置 TTL 后，Redis 怎样在内存、CPU 与及时清理之间取平衡？",
    "若为每个键创建独立定时器，海量键会带来调度成本；若完全等访问时再删，长期不访问的过期键会占内存。因此 Redis 把访问时检查与后台主动抽样结合起来。",
    "惰性过期发生在读取或操作键时：发现已到期便把它当作不存在并清理。主动过期周期性检查带 TTL 的键并删除到期样本，在时间预算内继续或停止。这个设计让过期清理只消耗受控 CPU，但到期键的物理内存回收可能稍晚。TTL 还会影响持久化、复制与故障切换的可见结果，具体实现随版本变化，工程上应依赖语义与监控而不是猜测内部扫描比例。",
    "缓存 1000 万个会话，统一在 12:00 到期会形成清理与回源波峰。把基础 TTL 设为 30 分钟并随机增加 0–5 分钟，使到期请求散开；访问一个已到期会话时惰性路径立即返回不存在，其余键由主动周期逐步回收。",
    "TTL 是数据新鲜度与内存生命周期，不是精确调度器。业务若要求 12:00:00 必须执行动作，应使用持久化调度机制；过期通知也可能延迟或丢失，不能作为唯一业务事实来源。",
    ["写入值与过期时间", "时间跨过截止点", "访问路径惰性判定", "后台主动抽样清理", "释放内存并触发回源"],
    ["先解释逐键定时器和纯惰性删除各自的成本。", "再把惰性与主动过期组合成受控清理过程。", "用集中到期说明为什么 TTL 要抖动。", "最后区分逻辑过期、物理回收与精确定时任务。"],
    "压测记录 `expired_keys` 增速、CPU、内存、命中率与回源 QPS；一次性写入同 TTL 与加入随机抖动各跑一轮，比较到期窗口内的峰值，而不是只看最终都被删除。",
    ("键的 TTL 已经显示到期，为什么进程内存未必在同一毫秒下降？", "逻辑上它已不可读，但物理清理由访问触发或后台周期完成；内存分配器也可能暂不把空间归还操作系统。TTL 语义与 RSS 下降不是同一个事件。"),
),
S(
    "32-cache-eviction", 32, "缓存", "缓存淘汰｜内存满时决定牺牲谁",
    "Redis 达到内存上限后，应该拒绝写入还是淘汰某些键？",
    "过期解决键何时失效，淘汰解决内存不足时如何腾空间，两者不能混为一谈。策略选择取决于哪些键可丢、是否都设置 TTL，以及访问频率能否代表未来价值。",
    "`noeviction` 保留现有数据并让会增加内存的写入失败，适合不能默默丢键的场景；`allkeys-*` 在全部键中选择，适合纯缓存；`volatile-*` 只从有 TTL 的键中选择，若候选不足仍可能写失败。LRU 倾向最近少用，LFU倾向频率低，random 简单，TTL 类策略偏向更早到期。Redis 的 LRU/LFU 是近似策略，抽样和元数据开销换取速度。大对象即使热门也可能挤走许多小对象，因此还要治理对象大小。",
    "实例上限 20GB，热数据 12GB、偶发数据 15GB。若使用 allkeys-lru，访问波动后会逐步保留近期集合；若业务键中混有不可丢的分布式锁或队列状态，任何全局淘汰都可能破坏正确性，应拆实例并对关键状态使用 noeviction 和持久化设计。",
    "策略名字不能替代容量模型。高命中率下仍可能发生写失败或延迟抖动；把缓存与关键协调状态混在同一淘汰域是常见风险。`volatile-lru` 在几乎没有 TTL 键时并不会神奇地从所有键腾空间。",
    ["达到 maxmemory 水位", "按候选集合选择策略", "近似采样选牺牲键", "释放空间后执行写入", "监控淘汰、失败与命中"],
    ["先分清过期与淘汰的触发条件。", "按 allkeys、volatile 和 noeviction 划定候选集合。", "再比较 LRU、LFU、随机与 TTL 倾向的工作负载假设。", "最后把关键状态隔离，并用对象大小和命中率验证选择。"],
    "对候选策略回放一天真实访问轨迹，统计命中率、evicted_keys、写失败、P99 和大对象占比。策略切换前确保 maxmemory 有安全余量，并准备在淘汰激增时限流回源。",
    ("为什么把 `volatile-lru` 简单理解为“内存满了就淘汰最久未访问的任意键”是错的？", "volatile 策略只在设置了过期时间的键中选择；无 TTL 键不在候选集合。若候选不足，写入仍可能失败。"),
),
S(
    "33-cache-patterns", 33, "缓存", "缓存读写模式｜把数据所有权与更新路径说清楚",
    "应用、缓存和数据库之间，谁负责读取、回源与写回？",
    "缓存模式描述职责边界，而不是某个固定产品。选择前要确认数据库是否是真相源、能容忍多旧的数据，以及失败时读写应该怎样退化。",
    "Cache Aside 由应用先读缓存，未命中再读数据库并回填；写入通常先改数据库再失效缓存，简单且常见。Read/Write Through 把回源或同步写交给缓存抽象层，应用更轻但实现复杂；Write Back 先写缓存后异步落库，写入快却把缓存升级为关键数据层；Refresh Ahead 在过期前主动刷新热门键，降低突发未命中。回填时应用 singleflight 或互斥，防止同一键并发击穿。",
    "商品详情读 10 万 QPS、更新每分钟一次。Cache Aside 命中时直接返回；未命中首个请求拿回填锁，读数据库后写 60 秒 TTL，其余请求等待或返回旧值。价格更新先提交数据库，再删除缓存；下一次读回填新价格。若允许最多旧 5 秒，也可后台刷新并在短窗口返回旧值。",
    "模式本身不保证一致性。Write Back 若缓存节点永久丢失会丢未落库数据；Cache Aside 的并发读写仍可能把旧值回填；缓存层故障时无限回源会压垮数据库。",
    ["明确数据库是真相源", "选择读命中与回源职责", "定义写入或失效顺序", "合并并发回填", "缓存故障时限流降级"],
    ["从数据所有权开始，区分缓存副本和主数据。", "比较 Cache Aside、Through、Back 与提前刷新的调用路径。", "用商品读多写少场景推演命中、失效和回填。", "最后补上并发竞态、缓存故障与回源保护。"],
    "为每种模式画失败表：缓存读失败、数据库读失败、缓存写失败、数据库写失败、进程崩溃。每个格子写清返回什么、是否重试、数据会旧多久和怎样修复。",
    ("Cache Aside 中数据库更新成功、删除缓存失败后，为什么不能只依赖下一次读自动恢复？", "缓存仍命中旧值，下一次读不会回源，旧数据可能一直保留到 TTL。需要重试失效、变更事件、较短 TTL 或版本校验。"),
),
S(
    "34-cache-consistency", 34, "缓存", "缓存一致性｜用时间线找出旧值重新出现的窗口",
    "数据库和缓存无法原子更新时，怎样限制不一致持续时间？",
    "一致性问题来自两个独立存储的操作顺序与并发交错。先定义允许的数据陈旧时间，再画读写时间线；没有一致性目标，就无法判断删缓存、更新缓存或版本控制是否足够。",
    "常见路径是先提交数据库，再失效缓存，因为直接更新缓存容易遗漏多种派生键。失败的失效要进入可靠重试或由 CDC/变更事件补发。并发读可能在写提交前读到旧库值、又在删除缓存后把旧值回填，因此可用带版本的缓存值、延迟二次失效、互斥回填或只允许新版本覆盖旧版本。强一致读应绕过缓存或等待版本，而不是让所有普通读付出同样代价。",
    "初始数据库版本 10、缓存为空。读 R 在写 W 提交前查到版本 10；W 提交版本 11并删除缓存；随后 R 才把版本 10 回填，旧值复活。若缓存写采用“仅当目标版本不大于 10 时写入”仍无法知道数据库已是 11；更稳妥的是回填前二次检查、按键互斥覆盖写，或由版本 11 的变更事件再次失效。",
    "延迟双删不是严格证明：延迟多久取决于真实读耗时、队列和故障。TTL 只限定旧值上界，无法满足写后立刻可见。任何方案都应说明失败重试与对账，而不是宣称绝对一致。",
    ["写数据库生成新版本", "提交后发送失效", "并发读可能握有旧快照", "回填时校验版本/互斥", "事件重试与 TTL 最终收敛"],
    ["先画数据库更新与缓存操作的先后，不从口号选方案。", "用旧读晚回填复现最隐蔽的竞态。", "再组合版本、失效事件、互斥和 TTL 限制窗口。", "最后为强一致请求设计绕过或版本等待。"],
    "并发测试用屏障精确控制：读到旧数据库值后暂停，写入新值并删除缓存，再释放旧读回填。断言最终缓存不能长期停在旧版本，并记录从写提交到收敛的最大时间。",
    ("为什么“先更新数据库，再删除缓存”仍不能单独证明最终不会出现旧缓存？", "并发读可能在数据库提交前读到旧值，却在删除发生后才回填。还需要互斥、版本、二次失效或变更事件来封住这个交错。"),
),
S(
    "35-cache-failures", 35, "缓存", "缓存三类失效压力｜穿透、热点击穿与雪崩要分开治理",
    "大量请求越过缓存打向数据库时，先判断是哪一种失效模式？",
    "三种现象都表现为回源上升，但根因不同：穿透反复查询不存在的数据；击穿是少数热点键同时失效；雪崩是大量键或整个缓存服务在同一窗口失效。治理必须对准请求分布。",
    "穿透用参数校验、空值短缓存、布隆过滤器和频率控制，最终仍以真相源为准；热点击穿用 singleflight、互斥回填、逻辑过期返回旧值或预热；雪崩用 TTL 抖动、多级缓存、集群隔离、回源限流和降级。缓存整体故障时，数据库保护优先于维持全部功能，非核心请求应快速失败或返回可接受的旧快照。",
    "商品 404 查询每秒 5 万次属于穿透，可缓存 30 秒空结果并限流；超级商品键在 12:00 到期、10 万请求并发属于击穿，只允许一个回填；100 万商品统一 TTL 在 12:00 到期属于雪崩，加入 0–10 分钟抖动并限制回源到数据库安全的 3000 QPS。",
    "布隆过滤器有误判，不能证明数据存在；空值缓存过长会让刚创建的数据暂时不可见；分布式锁失效时仍要有数据库限流。把所有键设置永不过期会把新鲜度和内存问题转移成更难发现的陈旧数据。",
    ["观察回源请求的键分布", "区分不存在/单热点/大面积失效", "选择空值/合并/抖动策略", "在数据库前限流降级", "恢复后预热并校验新鲜度"],
    ["先用键基数与热点集中度给三类问题分类。", "分别解释不存在、单键到期和批量失效的压力路径。", "为每类选择缓存内策略和数据库保护策略。", "最后演练缓存集群不可用，确认系统不会把全部流量直灌数据库。"],
    "故障看板同时显示请求总量、缓存命中、唯一未命中键数、Top Key、数据库 QPS 和拒绝量。唯一键数暴涨偏向穿透，单键占比极高偏向击穿，命中率整体骤降偏向雪崩或集群故障。",
    ("缓存命中率同样从 95% 跌到 20%，为什么不能直接套用同一个修复方案？", "要看未命中的键分布和原因。单个热点、海量不存在键、批量 TTL 与缓存集群故障的控制点不同，错误方案可能放大回源。"),
),
S(
    "36-redis-single-thread", 36, "缓存", "Redis 执行模型｜命令串行不等于整个进程只有一个线程",
    "Redis 常被称为单线程，它究竟串行了什么，又为什么仍会出现阻塞？",
    "对经典 Redis 工作负载，关键数据结构操作主要在一个命令执行路径中串行，避免了大量细粒度锁；但网络 I/O、持久化、异步释放等能力可使用其他线程或子进程，具体边界随版本和配置变化。",
    "事件循环监听连接、读取请求、执行就绪命令并写回响应。内存访问快、数据结构经过优化，加上少锁竞争，使短命令吞吐很高；多路复用只是让一个执行路径同时管理很多连接，不会让一条慢命令自动并行。大键删除、复杂集合操作、长 Lua 脚本、阻塞式系统调用或内存换页都会占住执行路径，让其他请求排队。性能诊断应看命令耗时、慢日志、大键、事件循环延迟和 CPU，而不是背“单线程所以快”。",
    "每秒 8 万个 GET 平均 50 微秒时，执行路径可顺畅轮转；若一个集合运算持续 200 毫秒，这期间到达的约 1.6 万个请求都会等待，P99 突然升高。把大集合拆批、改异步释放或预计算，往往比盲目增加客户端连接更有效。",
    "单命令原子只覆盖该实例内该命令的执行，不等于跨多个命令、多个键槽或外部数据库的业务事务原子。连接越多也不会突破单个热点分片的 CPU 上限，反而可能增加排队和内存。",
    ["连接事件进入就绪队列", "读取与解析请求", "命令执行路径串行修改数据", "响应写回客户端", "后台任务处理持久化/释放等工作"],
    ["先限定“单线程”指的执行边界，避免绝对化。", "再解释事件循环和内存操作为何适合短命令。", "用一个 200ms 慢命令推导所有连接的排队。", "最后列出大键、慢脚本、换页和后台任务的观测方法。"],
    "实验分别执行短 GET、渐增集合运算和大键删除，记录事件循环延迟与 P99。若单个命令耗时与全局延迟峰值同步，就能直接看到串行路径的队头阻塞。",
    ("为什么把客户端连接从 100 增加到 1000，未必能解决单个 Redis 分片 CPU 已满的问题？", "连接增加的是排队与并发请求来源，不会让串行命令执行路径获得更多核心。应减少单命令成本、拆热点或水平分片。"),
),
S(
    "37-redis-distributed-lock", 37, "缓存", "Redis 分布式锁｜从互斥租约走到过期与旧持有者风险",
    "多个进程竞争同一资源时，怎样用 Redis 建立有期限、可验证归属的互斥？",
    "分布式锁不是一个永久布尔值，而是带唯一持有者标识和有效期的租约。获取、释放、续期与业务副作用之间都存在故障窗口；目标不仅是“多数时候互斥”，还要阻止过期旧持有者继续写坏数据。",
    "单实例常用 `SET lock_key token NX PX ttl` 原子获取，token 每次唯一。释放必须原子比较 token 再删除：新版本 Redis 可用条件删除能力，兼容旧版本常用 Lua 脚本。TTL 防止持有者崩溃后永久占锁，续期只能延长仍属于自己的租约。若业务执行可能超过 TTL，旧持有者在暂停后恢复会与新持有者并行；关键资源应由递增 fencing token 在数据库或存储端拒绝旧序号。多节点 Redlock 需要评估时钟、网络和故障模型，不能替代资源端约束。",
    "进程 A 获取 token=`a7`、TTL 10 秒和 fencing=41，随后暂停 15 秒。锁过期后 B 获取 token=`b9`、fencing=42 并写库存。A 恢复时即使还以为自己持锁，库存服务看到 41 小于已接受的 42，拒绝它的写入。若只有 token 比较删除，A 不会误删 B 的锁，但仍可能在过期后执行副作用。",
    "锁的 TTL 不能凭平均耗时设定，应覆盖尾延迟并有续期/中止策略。主从故障切换、时钟跳变与网络分区会改变安全假设；对资金、库存等关键写入，最终正确性必须由数据库条件更新、唯一约束或 fencing 保护。",
    ["生成唯一 token 并原子获取租约", "执行业务并监控剩余 TTL", "必要时仅为自己续期", "资源端校验 fencing 序号", "原子比较 token 后释放"],
    ["从 `NX+TTL+唯一 token` 建立最小租约模型。", "解释为什么释放必须比较归属，续期也必须验证持有者。", "用进程暂停超过 TTL 复现双持有者风险。", "最后加入 fencing 与资源端条件，说明多节点方案的故障假设。"],
    "故障测试在获取后、业务中、过期后和释放前暂停进程，并模拟 Redis 故障切换。验收条件是：不会误删新锁；过期旧持有者的副作用被资源端拒绝；锁等待与持有时长有上界和告警。",
    ("唯一 token 已能防止 A 误删 B 的锁，为什么仍可能需要 fencing token？", "唯一 token 只保护 Redis 中的释放。A 的租约过期后仍可能继续写外部资源；递增 fencing 让资源端识别并拒绝旧持有者。"),
),
S(
    "38-cache-architecture", 38, "缓存", "缓存架构全景｜按数据新鲜度与故障半径设计多级防线",
    "从进程本地到 Redis 集群，怎样组合多级缓存而不让一致性失控？",
    "多级缓存把热点挡在更靠近请求的位置，但每多一层就多一个副本、失效路径和容量边界。先给数据分类：是否可丢、最大陈旧时间、热点分布、对象大小和回源代价。",
    "请求可依次经过浏览器/CDN、网关、进程本地缓存和远程缓存，再回到数据库。近端层延迟低但副本多，本地失效通常依赖短 TTL 或广播；远程层便于集中治理，可用分片和副本扩展。读路径要防穿透与击穿，写路径用版本/失效事件收敛；集群故障时由限流、旧值降级和非核心功能关闭保护数据库。预热只加载真正热点，并设置容量水位，避免启动时制造雪崩。",
    "商品详情允许旧 10 秒：进程本地缓存 TTL 2 秒，Redis TTL 60 秒并带版本，数据库是真相源。更新后发送版本 18 的失效事件；本地层最晚 2 秒自然收敛，Redis 删除失败由事件重试。Redis 全部不可用时，网关把回源限制到 3000 QPS，热点可返回带“数据可能稍旧”标记的快照。",
    "多级命中率不能简单相加，也不能只看总体命中率；本地层可能长期保存旧值，Redis 热点分片可能单点饱和。缓存中若混入不可丢状态，需要拆分故障域和淘汰策略。",
    ["按新鲜度和可丢性分级", "近端缓存吸收最热请求", "远程缓存集中共享副本", "失效事件与版本逐层收敛", "故障时限流、旧值和降级保护回源"],
    ["先按数据语义分层，而不是按中间件堆叠。", "逐层计算延迟、容量、命中与最大陈旧窗口。", "把更新事件、TTL 和版本串成失效路径。", "最后演练某一层和全缓存故障，验证回源限流与降级。"],
    "架构表为每层填写 `容量、P99、命中率、TTL、可接受旧值、淘汰策略、故障回退、所有者`。再计算在各层同时失效时数据库接到的最大 QPS，必须小于压测安全水位。",
    ("为什么增加进程本地缓存后，远程缓存 QPS 降了，一致性却可能更难？", "每个进程多了一份独立副本，集中删除 Redis 不能自动清掉它们。需要短 TTL、版本校验或可靠广播，并接受明确的新鲜度窗口。"),
),
S(
    "38a-mock-cache", 38.5, "缓存", "缓存专题复盘｜从一次热点失效推演到数据库保护",
    "怎样把过期、淘汰、读写模式、一致性和故障治理连成一次完整推演？",
    "缓存专题的主线不是命令，而是副本生命周期：何时创建、何时过期、内存不足时谁被牺牲、写入后怎样失效、缓存不可用时谁保护真相源。",
    "读请求先经过近端与 Redis，未命中由合并回填控制并发；TTL 加随机抖动，淘汰策略只作用在明确可丢的缓存实例。更新先提交数据库并发出版本化失效事件，失败可重试。热点过期时允许一个请求回源，其他请求等待或读旧值；集群故障时限流、降级和静态快照共同保护数据库。",
    "商品 S9 平时 50k QPS，Redis TTL 到期时 2 万并发到达。singleflight 只允许一个数据库查询，其他请求最多等 100ms 后读取上一版；更新事件版本 31 到达后，只允许 31 或更高版本写缓存。此时 Redis 节点又触发淘汰，监控发现大对象挤压，团队将不可丢锁状态迁出缓存实例并限制对象大小。",
    "复盘不能把所有保护都默认开启。互斥会增加等待，旧值会牺牲新鲜度，空值可能遮住新建数据，短 TTL 会增加回源；每个选择都要对应具体 SLO。",
    ["请求命中或合并回填", "TTL 与版本控制生命周期", "内存水位触发可控淘汰", "失效事件收敛副本", "全层故障时保护数据库"],
    ["先跟踪一个键从数据库回填到过期或淘汰。", "插入并发更新，画出旧值晚回填的竞态。", "再注入热点过期和缓存节点故障，说明合并、旧值与限流。", "最后检查关键状态是否被错误放入可淘汰域。"],
    "选择一个 Top Key 做演练，收集每层命中、回源并发、锁等待、缓存版本、淘汰原因和数据库 QPS。只有当缓存完全旁路时数据库仍被限制在安全水位，故障预案才算闭环。",
    ("缓存专题中最先要确定的为什么不是 TTL 应设多少秒？", "应先确定数据所有权、允许陈旧时间、峰值回源能力和故障回退。TTL 只是这些约束下的一根控制杆，脱离语义没有统一正确值。"),
),
S(
    "39-elasticsearch-ha", 39, "NoSQL", "Elasticsearch 高可用｜让分片副本、写入确认与恢复策略互相匹配",
    "节点故障时，Elasticsearch 怎样继续读写，并避免恢复流量拖垮在线请求？",
    "索引被拆成主分片和副本分片，节点承载分片，集群管理角色维护元数据与分配。高可用不是多放几台机器，而是让副本分散、选主可达、确认语义和容量余量同时成立。",
    "请求先到协调节点，写入按路由找到主分片；主分片处理后复制到副本，并按确认条件回复。节点离开后，符合条件的副本可提升，集群重新分配缺失副本。跨可用区部署要平衡故障隔离与网络延迟，专用管理节点避免数据压力干扰集群决策。恢复时应限制作业并发和带宽，优先保持搜索与写入 SLO；快照用于应对误删和集群级灾难，因为副本会同步逻辑错误。",
    "一个索引 6 个主分片、每个 1 个副本，均匀分布在 3 个数据节点。节点 N2 故障后，其上的主分片由其他节点副本接替，但副本数暂时不足，集群处于可服务却冗余降低的状态。若立刻全速重建 2TB 数据，磁盘和网络可能挤压查询，因此按在线负载限速，并先恢复关键索引。",
    "有副本不等于任何故障都不丢数据；确认边界、节点同时故障和未完成复制都影响结果。把管理、协调和数据角色绝对固定也不是通用答案，应按规模和负载决定。副本更不是防误删备份。",
    ["协调节点解析请求", "路由到目标主分片", "主分片处理并复制", "故障时提升合格副本", "限速重建并用快照兜底"],
    ["先建立集群、节点、索引、分片四层关系。", "沿一次写入说明路由、主分片与副本确认。", "再推演节点故障、分片提升和冗余恢复。", "最后加入容量余量、恢复限速、跨区延迟和快照。"],
    "演练关停一个数据节点，记录集群状态、不可分配分片、写入/查询 P99、提升时间和恢复带宽。验收不仅是“最终变绿”，还要确认故障窗口无超出目标的数据损失和在线延迟。",
    ("为什么一个索引配置了副本，仍必须做快照？", "副本面向节点故障并同步当前索引状态，误删、错误写入或整个集群损坏可能传播到所有副本；快照提供独立恢复点。"),
),
S(
    "40-elasticsearch-query", 40, "NoSQL", "Elasticsearch 查询优化｜从倒排索引、分片扇出到深分页成本",
    "搜索变慢时，怎样判断问题在映射、查询、分片还是资源？",
    "搜索请求先被协调节点拆到相关分片，每个分片用倒排索引找候选、计算过滤或相关性，再归并结果。慢查询可能来自单分片做太多，也可能来自分片太多导致扇出与归并。",
    "字段映射先决定哪些内容需要分词、精确过滤、排序或聚合；精确条件优先放过滤上下文以复用结果并避免无谓评分。只取所需字段，批量写入并设置符合新鲜度要求的 refresh 策略。深分页若用 `from+size`，每个分片都要保留大量候选；稳定遍历更适合 `search_after` 配合唯一排序键，强一致翻页可结合 PIT 等机制。分片大小、数量、堆、页缓存和禁止 swap 共同影响尾延迟。",
    "20 个分片查询第 5001 页，每页 20 条，朴素方式每片可能保留 100020 个候选，协调层面对约 200 万候选再归并。改为按 `(event_time,_id)` 使用上一页游标，每片只取下一小批候选，成本不再与页码线性增长。若只查一个租户，写入时使用路由还能减少扇出。",
    "`search_after` 适合顺序浏览，不支持低成本任意跳页；PIT 也有资源成本和生存时间。把所有字段都设为可聚合会放大索引与内存，分片过小同样会增加元数据和协调开销。",
    ["协调节点确定目标分片", "倒排索引筛选候选", "过滤/评分与字段读取", "跨分片归并排序", "返回结果与下一游标"],
    ["先沿查询执行路径定位单分片计算和跨分片扇出。", "再从 mapping、filter、字段读取和聚合减少工作量。", "用候选量公式解释深分页，并替换为稳定游标。", "最后检查分片布局、堆、页缓存、写入刷新和资源竞争。"],
    "为慢查询保存查询体、命中分片数、每分片耗时、候选量、返回字段大小和 profile 结果。优化前后同时比较 P50/P99 与资源，避免平均值掩盖某个热点分片。",
    ("为什么把索引从 5 个分片增加到 50 个，查询不一定更快？", "并行度增加的同时，每次查询的网络扇出、分片固定开销和协调归并也增加；小数据集或全索引搜索可能更慢。"),
),
S(
    "41-mongodb-ha", 41, "NoSQL", "MongoDB 高可用｜复制集负责冗余，分片集群负责水平扩展",
    "MongoDB 中复制集、分片、配置服务和 mongos 分别承担什么状态？",
    "复制集是一组维护同一数据副本的节点，通过选举产生主节点；分片集群把不同数据范围分给多个分片，每个分片通常又是复制集。配置服务器保存分片元数据，mongos 根据元数据路由请求。",
    "写入主节点并进入操作日志，副本异步重放；write concern 决定客户端等待何种确认，read concern 与读偏好决定读取可见性和节点选择。多数派故障域设计要避免一个机房故障带走多数投票；仅投票的仲裁节点不保存业务数据，不能替代数据副本。分片键影响均衡与查询路由，均衡器迁移数据时要控制对在线流量的影响。备份仍独立于复制。",
    "三个数据节点分布为 A 区 1 个、B 区 1 个、C 区 1 个，任一区故障仍有两个节点形成多数。若改成 A 区 2 个、B 区 1 个，A 区故障只剩一个节点，无法选主。一个分片集群有 4 个分片，每个分片都要单独检查复制多数与容量，mongos 本身不存业务文档。",
    "读副本可能读到旧数据，选举期间写入会短暂不可用；提高 write concern 会改变延迟和可用性。分片不是备份，配置服务器也不是普通业务查询节点。跨地域部署必须用实测网络延迟评估多数派写入成本。",
    ["mongos 读取路由元数据", "请求定位目标分片", "分片主节点写入 oplog", "副本复制并满足 concern", "故障选举或均衡迁移"],
    ["先区分复制集的冗余与分片的扩展。", "再说明主节点、oplog、选举和多数派的关系。", "把配置服务器、mongos 与数据分片放入同一请求路径。", "最后用多可用区布局、concern、备份和迁移验证故障模型。"],
    "故障演练分别停止 mongos、单个副本节点、当前主节点、一个可用区和配置服务器成员；记录读写是否成功、选举时间、数据新鲜度与恢复动作，避免只测试最轻的单节点故障。",
    ("为什么增加一个仲裁节点可以帮助投票，却不能等价于增加一个数据副本？", "仲裁节点参与选举但不保存业务数据，不能承担读取、接替完整数据或提高数据冗余度。"),
),
S(
    "42-mongodb-performance", 42, "NoSQL", "MongoDB 查询优化｜让索引顺序服务真实的等值、排序与范围访问",
    "复合索引字段应该怎样排序，查询为什么仍可能回表或广播？",
    "查询先经过路由，再由目标分片的规划器选择索引，扫描键并在需要时读取文档。优化要同时减少目标分片数、扫描索引键数、读取文档数和排序工作。",
    "常用 ESR 思路把等值字段放前面，再按工作负载在排序与范围之间选择。若避免内存排序更重要，可用 Equality→Sort→Range；若范围条件极具选择性，Equality→Range→Sort 可能扫描更少，但排序可能另行完成。覆盖查询只从索引返回所需字段，减少文档读取。分片查询包含分片键可定向路由，否则 mongos 可能广播。大数组、无限增长文档和过多索引会放大写入与搬迁成本，应按访问模式建模。",
    "查询 `{tenant:7, status:'PAID', created_at:{$gt:T}}`，按 amount 排序并取 20 条。若大多数记录都很新但 amount 排序必须稳定，可评估 `(tenant,status,amount,created_at)`；若时间范围只命中万分之一，`(tenant,status,created_at,amount)` 扫描更少但可能需要排序。用 explain 比较 keysExamined、docsExamined 与是否出现阻塞排序，而不是机械套公式。",
    "ESR 是指南，不是不可变定律；低选择性、排序方向、多键索引和分片路由都会改变结果。索引越多读选择越丰富，写入和缓存成本也越高。把 MongoDB 当作关系数据库逐表照搬，常会错失按聚合边界嵌入文档的优势。",
    ["mongos 判断是否定向路由", "规划器比较候选索引", "索引扫描过滤候选", "必要时读取文档或排序", "返回结果并记录执行统计"],
    ["先从查询形状与分片键确定会访问多少分片。", "再按等值、排序、范围分析复合索引顺序。", "用 explain 的扫描量和排序验证 ESR 或 ERS 选择。", "最后治理覆盖查询、大文档、数组和索引写放大。"],
    "保存 Top 查询形状及 `nReturned、keysExamined、docsExamined、executionTime、targetedShards、hasSortStage`。每次加索引还要记录写入耗时、索引大小和构建影响，避免只优化一条读路径。",
    ("为什么复合索引设计不能永远机械地按 Equality→Sort→Range 排列？", "如果范围条件极具选择性，把它提前可能大幅减少扫描；代价是排序可能无法完全由索引满足。应按实际数据分布与 explain 结果在 ESR 和 ERS 间选择。"),
),
S(
    "42a-mock-nosql", 42.5, "NoSQL", "NoSQL 专题复盘｜从访问模型选择检索系统与文档数据库",
    "面对搜索、聚合、灵活文档和高可用需求，怎样判断系统边界而不是跟随产品标签？",
    "Elasticsearch 与 MongoDB 都能查询 JSON 风格数据，但核心访问结构不同：前者围绕倒排索引、相关性和分片搜索，后者围绕文档主存储、复合索引、复制集与分片路由。选择要从真相源、查询形状和一致性要求出发。",
    "全文检索、相关性、复杂聚合和日志探索更接近 Elasticsearch；以文档为聚合边界、需要主数据读写与灵活结构时可评估 MongoDB。常见架构让事务数据库或文档库保有真相，变更事件同步搜索索引，并明确索引延迟、重建和对账。两边都要规划分片键/路由、热点、分片大小、副本、备份与故障恢复。",
    "商品主数据写 MongoDB，商品名与属性同步到 Elasticsearch。更新版本 208 先在主库提交，再通过 Outbox 投递；搜索结果可能短暂仍是 207，详情页按商品 ID 回主库读取 208。对账发现索引版本落后时重放事件，而不是允许搜索集群直接改写主数据。",
    "双系统会带来延迟、重复、乱序和重建成本。如果查询只按主键且数据规模普通，引入搜索集群可能过度设计；如果需要复杂相关性，却用文档库堆大量正则查询，也会把成本转移到线上。",
    ["列出主写与查询形状", "确定唯一真相源", "选择文档索引或倒排检索", "版本化事件同步派生副本", "对账、重建与故障演练"],
    ["先用访问模型而不是数据长相选择系统。", "分别画 Elasticsearch 分片搜索与 MongoDB 路由查询路径。", "再设计主数据到搜索索引的版本化同步。", "最后验证副本、快照、跨区和全量重建时间。"],
    "决策记录至少比较：主写语义、事务范围、全文相关性、聚合、定向路由、允许索引延迟、全量重建时间、团队运维能力。若某项没有实测数字，就标记为未验证假设。",
    ("为什么“两个系统都能存 JSON”不足以支持技术选型？", "数据表示相似不等于执行模型相同。应比较倒排检索、文档事务、索引写放大、查询路由、一致性与恢复路径。"),
),
S(
    "43-final-test", 43, "综合", "综合演练｜用一场流量突增验证整条后端链路",
    "如何把服务治理、数据库、消息、缓存和 NoSQL 放进同一个可验证方案？",
    "综合题的价值不是堆中间件，而是从 SLO 和故障模型出发，让每一层只解决明确问题，并交代新增状态、降级方式、恢复步骤和验证指标。",
    "先写峰值流量、用户延迟、正确性、RPO/RTO 和成本边界；入口用限流与幂等令牌保护核心链路，服务调用分配超时预算并隔离外部依赖。数据库以本地事务保存事实与 Outbox，缓存只作为可失效副本，消息队列削峰并以业务键维持实体内顺序，消费者幂等。搜索索引从事件派生，允许明确延迟并可重建。每个异步状态都有查询、重试、死信和对账入口。",
    "大促峰值 100k 请求/秒，数据库安全写入 8k/秒。网关先把无资格请求挡掉，库存令牌只放 12k/秒进入队列；订单消费者按 7k/秒落库，差速可控。缓存故障时回源被限制到 3k/秒；支付服务 300ms 超时后不盲目重试扣款，而是按支付单号查询结果。搜索索引延迟 5 秒可接受，订单详情则回真相源。",
    "任何“自动扩容、最终一致、高可用”都必须落成数字与状态机。若数据库已满，单纯把入口改异步只会积压；若幂等键定义错，重试会重复副作用；若恢复没有优先级，重建流量可能压垮在线服务。",
    ["写 SLO、容量与故障模型", "入口限流/幂等/超时隔离", "本地事务与缓存副本", "消息削峰和幂等消费", "搜索派生、对账与灾难恢复"],
    ["从用户动作画同步关键路径与异步旁路。", "为每一跳标注超时、容量、重试和幂等键。", "依次注入下游超时、数据库变慢、消息积压、缓存全挂和索引落后。", "最后用 RPO/RTO、业务对账与降级体验评价方案。"],
    "提交一张架构图和一张故障表。故障表每行包括 `故障、检测信号、自动动作、用户看到什么、数据是否会丢、恢复步骤、负责人`；再用压测数据证明各限流阈值小于下游安全容量。",
    ("综合架构中为什么要先写 SLO 和安全容量，再选择中间件？", "同一技术在不同延迟、一致性和故障要求下取舍不同。没有数字就无法确定限流、超时、缓存陈旧窗口、消息积压上限和恢复优先级。"),
),
S(
    "44-closing", 44, "综合", "学习收束｜把知识点沉淀成可复用的工程判断",
    "学完 50 篇后，怎样避免只记住术语，却不会定位真实问题？",
    "可迁移能力来自一套稳定推理顺序：先描述现象与目标，再追踪状态和时间线，量化容量，识别故障边界，选择机制，最后用实验和监控反证。产品会升级，这套方法比参数记忆更耐久。",
    "每个专题都可以归到四类控制：流量控制决定多少工作进入系统；状态控制决定事实写在哪里、如何并发与复制；时间控制处理超时、过期、延迟和重试；故障控制负责隔离、降级、恢复与对账。复习时不要问“某组件有什么特性”，而要问“它在哪个边界承诺了什么，失败后剩下什么状态，谁来收敛”。",
    "遇到缓存旧值：先画数据库提交、缓存失效、并发回填时间线；遇到消息积压：先算生产减消费的差速与清空时间；遇到服务雪崩：先画调用链、超时预算和共享资源池；遇到查询变慢：先看访问路径、扫描量和数据分布。四个问题使用的是同一套证据驱动方法。",
    "完整学习不等于每项都自行实现，也不等于一次读完。某些机制受版本、部署和业务语义影响，必须回到官方文档、压测和故障演练校准；对不确定结论保留条件比给出绝对答案更专业。",
    ["描述现象与可量化目标", "画状态/时间/依赖边界", "提出机制与关键假设", "用实验和故障注入验证", "沉淀指标、复盘与下一轮问题"],
    ["从五个专题各选一个真实问题，不看标题重新推导。", "给每个方案写出前提、收益、代价和失效边界。", "用一个最小实验或故障演练验证最关键假设。", "把结果更新到自己的运行手册，而不是停在概念笔记。"],
    "建立个人工程判断卡：正面写问题与约束，背面写状态流、容量公式、三个故障点、两个可替代方案和一个验证实验。每周抽取一张，用新案例重做，答案应随证据而不是随记忆更新。",
    ("判断自己真正掌握一个机制，最可靠的标准是什么？", "能在给定约束下从状态与时间线推导它，说明适用边界和代价，并设计实验或故障注入验证；仅能复述定义还不够。"),
),
]
